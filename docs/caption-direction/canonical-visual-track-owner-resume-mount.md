# Canonical Visual and Track Owner Resume Mount

Milestone: `POST-CAP-20-CANONICAL-VISUAL-TRACK-RESUME-MOUNT`

Status: `passed_source_integration`

## Outcome

The canonical Visual Intelligence and Track All/SAM 3.1 Caption support
services now continue through the existing one-writer sequential-resume ledger
after persisting and rereading their authenticated evidence. The same Caption
job is resumed with only the current owner's projected artifact; prior owner
artifacts remain canonical inputs.

The focused positive fixtures now complete real Caption runtime calls rather
than stopping at an evidence record. This is source-level integration evidence;
it does not claim a new Gemini call, GPU execution, model run, media render, or
terminal private qualification.

## Corrected lineage gaps

- Visual Intelligence owner projection now binds the authenticated reread
  result, while the immutable report remains separately bound in the evidence
  record.
- Caption runtime accepts the declared rendered-inspection artifact type for a
  `rendered_caption_inspection` packet and the occupancy artifact type for a
  `final_frame_occupancy` packet. The two purposes are no longer collapsed.
- Both owner services reread the exact persisted Caption evidence record before
  the specialist runtime is called.

## Ownership

The existing canonical owners remain unchanged. Caption does not dispatch
Gemini or SAM 3.1, run a tracker, create assets, mutate timing, approve QA,
settle costs, deliver publicly, or gain production authority. The shared
resume repository remains the only sequential continuation writer.

## Verification

- canonical Caption Visual Intelligence support smoke: 17 assertions;
- canonical Caption Track All support smoke: 16 assertions, plus the canonical
  68-check SAM task/result owner proof;
- both positive paths persist and reread step 1 and finish their exact Caption
  planning jobs;
- full server typecheck and repository regressions are required before this
  milestone is frozen.

# Project Edit Brief Attachment Boundary

Marker Attachments are mock/local and metadata-only. They are useful for UI planning, marker intent capture, and future planner hints, but they are not durable production media records.

Reference URL metadata is never fetched. Upload controls are disabled. Audio and SFX attachment kinds never start sound runtime. no upload, no file bytes, no URL fetch, no media processing, no sound runtime, no providers, no workers, no render, no credits, and no Supabase command or write occurred in RP-EDITBRIEF-08.

Production remains blocked on storage/media gates, source asset lifecycle, authenticated persistence, worker/runtime approvals, privacy review, and owner approval.

## RP-EDITBRIEF-12 Verification

Metadata-only marker attachments are now covered by the consolidated Edit Brief E2E smoke and Playwright path. Attachments remain labels, notes, and URL metadata only: no upload, file-byte read, signed URL, external URL fetch, media processing, sound runtime, provider/model, worker, render/export/progress, credit, Supabase command, or migration is enabled.

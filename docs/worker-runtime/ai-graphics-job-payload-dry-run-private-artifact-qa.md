# AI Graphics Job Payload Dry-Run Private Artifact QA

Decision: `worker_ai_graphics_metadata_job_payload_dry_run_qa_passed_with_warnings`

Private artifact and checksum refs are `accepted_with_warnings`. The QA boundary requires placeholder-only refs such as `<PRIVATE_ARTIFACT_MANIFEST_REF>` and `<CHECKSUM_REF>`, private artifact scope only, and no signed URL or public artifact source-of-truth path.

No worker execution, job claim, lease mutation, queue execution, route execution, actual tool execution, provider/model runtime, browser/WebGL/canvas runtime, resvg rasterization, Remotion render/export, Supabase mutation, SQL execution, GCS/storage transfer, signed URL creation, public artifact creation, raw prompt execution, internal beta unlock, external beta unlock, production unlock, or broad service-role handler was enabled.

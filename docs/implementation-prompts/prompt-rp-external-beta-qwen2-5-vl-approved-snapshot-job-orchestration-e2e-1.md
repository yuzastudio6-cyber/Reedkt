# RP-EXTERNAL-BETA-QWEN2_5_VL_APPROVED_SNAPSHOT_JOB_ORCHESTRATION_E2E_1

Implement the next guarded backend orchestration packet after Qwen2.5-VL product-route runtime readiness rollup.

Required boundaries:

- use only approved snapshot references;
- require approval record, credit reservation, queue lease, worker id, idempotency key, private manifest references, and checksum references;
- backend-only provider/model access;
- no frontend provider/model call;
- no arbitrary private/user media;
- no public artifacts or signed URLs unless a later artifact-access packet approves them;
- no external beta unlock, paid production unlock, or final render/export in this packet.

Use #1403 as the accepted product-route provider runtime evidence and keep #577 excluded unless it is repaired and externally validated.

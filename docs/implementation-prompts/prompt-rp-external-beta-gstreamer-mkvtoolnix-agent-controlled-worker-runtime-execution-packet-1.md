# RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-AGENT-CONTROLLED-WORKER-RUNTIME-EXECUTION-PACKET-1

Use only after `RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-AGENT-CONTROLLED-WORKER-DISPATCH-DRY-RUN-1` is merged with decision `completed_gstreamer_mkvtoolnix_agent_controlled_worker_dispatch_dry_run_metadata_only`.

The next packet may plan a separately guarded worker runtime execution packet for GStreamer/MKVToolNix. It must preserve the approved snapshot, approval record, no-spend or reserved-credit policy, job, worker lease, route idempotency, dispatch idempotency, queue idempotency, dispatch dry-run idempotency, command-template allowlist, private input manifest, output manifest schema, QA report schema, cleanup policy, retention policy, failure policy, retry policy, non-public artifact policy, audit refs, and runtime evidence references.

Any runtime execution must require an explicit confirmation gate and must name the exact generated/private fixture scope. It must not broaden to arbitrary user media, public URLs, signed URL source-of-truth, final render/export, broad external beta, paid production, production, Supabase mutation, SQL execution, FFmpeg/FFprobe, Docker push/deploy, Remotion, provider/model calls, or public artifacts.

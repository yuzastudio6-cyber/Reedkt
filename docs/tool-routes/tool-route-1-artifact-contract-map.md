# TOOL-ROUTE-1 Artifact Contract Map

- `provider_planning_manifest`: provider planning manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `approved_plan_snapshot_candidate`: approved plan snapshot; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `worker_dry_run_job_plan`: worker dry-run job plan; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `web_source_capture_extraction_manifest`: web source/capture/extraction manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `map_geojson_style_camera_timing_render_manifest`: map GeoJSON/style/camera/timing/render manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `ai_tools_visual_asset_manifest`: AI Tools visual asset manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `track_a_render_export_manifest`: Track A render/export manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `track_b_media_analysis_manifest`: Track B media analysis manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `sound_music_audio_cue_manifest`: Sound/Music/Audio cue manifest; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `supabase_metadata_placeholder`: Supabase metadata placeholder; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `observability_cost_event_placeholder`: observability/cost event placeholder; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `compliance_review_placeholder`: compliance review placeholder; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `frontend_ux_route_placeholder`: frontend UX route placeholder; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `billing_credit_placeholder`: billing/credit placeholder; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.
- `public_artifact_signed_url_delivery_blocked_placeholder`: public artifact/signed URL delivery blocked placeholder; private only `true`; public artifact allowed `false`; signed URL source-of-truth allowed `false`.

No Supabase mutation, SQL execution, Google Cloud API call, Secret Manager API call, provider call, model call, tool execution, worker execution, route execution, browser capture, Docker/Cloud Run execution, storage transfer, signed URL creation, public artifact creation, credit mutation, Stripe checkout/webhook/payment processing, deployment, internal beta unlock, external beta unlock, production unlock, dependency mutation, raw prompt execution, final render/export, or broad service-role handler was enabled.

# WORKER_RUNTIME_JOBS-SOUND-CPU-COMPLIANCE-SECURITY-GAP-CLOSURE: Close Compliance Security Gap, No Execution

Use `worker_runtime_jobs_sound_cpu_billing_stripe_credits_gap_closure_completed_with_warnings_ready_for_compliance_security_gap_closure` as source evidence. Create a no-execution packet that evaluates whether repo-lane compliance, privacy, security, secret-boundary, service-role-boundary, artifact-retention, and audit planning evidence can close the compliance/security planning gap, while preserving worker execution, media processing, Supabase mutation, SQL execution, storage writes, signed URLs, artifact delivery, credit mutation, Stripe checkout/webhook/payment processing, beta, and production as blocked.

Do not run workers, run routes, run tools, dispatch jobs, claim leases, open/process/write media, run FFmpeg/ffprobe, run Docker build/run/push, call GCP/Cloud Run/Secret Manager, touch Supabase, execute SQL, write storage objects, create artifacts, create signed/public URLs, call providers/models, mutate credits/Stripe, run Stripe checkout/webhooks/payment processing, unlock beta, unlock production, deploy, execute raw prompts, final render/export, or enable broad service-role handlers.

Require duplicate guards before creating a branch or PR. Check for same-head and same-purpose open PRs because other Codex chats may be working adjacent lanes. If a duplicate or superseding branch/PR exists, stop and report it instead of creating another packet.

Preserve `toolCandidateCount: 15`, `closedGapCountToday: 6` as source evidence, `remainingGapCount: 2`, `workerDispatchAllowed: false`, `claimLeaseAllowed: false`, `workerExecutionAllowed: false`, `mediaProcessingAllowed: false`, `supabaseMutationAllowed: false`, `sqlExecutionAllowed: false`, `storageWriteAllowed: false`, `signedUrlCreationAllowed: false`, `artifactDeliveryAllowed: false`, `creditMutationAllowed: false`, `stripeCheckoutAllowed: false`, `stripeWebhookAllowed: false`, `paymentProcessingAllowed: false`, `externalBetaAllowed: false`, and `productionUnlockAllowed: false`.

# RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-NARROW-EXTERNAL-AGENT-RUNTIME-GUARDED-ROUTE-WORKER-CONTROLLED-WORKER-ROUTE-WORKER-SOURCE-EXECUTION-PACKET-1

Implement the next source packet only after reading the QA rollup:

- `RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-NARROW-EXTERNAL-AGENT-RUNTIME-GUARDED-ROUTE-WORKER-CONTROLLED-WORKER-RUNTIME-INTEGRATION-IMPLEMENTATION-QA-ROLLUP-1`

The next packet may define an explicit confirmation gate for a narrow generated-fixture-only route/worker source execution packet. It must not broaden into private/user media, public artifacts, final export, broad external beta, paid production, production, Supabase mutation, SQL, provider/model calls, Docker, FFmpeg/FFprobe, or unrestricted tool execution.

Required boundaries:

- Keep source class generated-fixture-only unless a later owner decision changes it.
- Require an explicit environment confirmation before any source execution path can run.
- Keep route execution, worker dispatch, worker process start, worker lease claim, persistent job queue write, GStreamer execution, and MKVToolNix execution separately recorded and guarded.
- Preserve product-ready end-to-end local OSS tools as `0`.

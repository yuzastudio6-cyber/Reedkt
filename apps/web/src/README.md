# apps/web/src

This directory is a Phase 44B scaffold only.

The active React source remains in root `src` until the UI/backend source split
is safe to move. Do not place server workers, service-role clients, model
weights, provider execution, Cloud Run job execution, or heavy tool runtime code
here.

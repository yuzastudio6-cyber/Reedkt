# WORKER_RUNTIME_JOBS-SOUND-CPU-CONTROLLED-NO-MEDIA-NO-ARTIFACT-TOOL-CALL-READINESS-PROOF: Run Limited Synthetic SOUND CPU Tool-Call Readiness Proof, No Media/Artifacts

Use `worker_runtime_jobs_sound_cpu_limited_no_media_no_artifact_tool_call_readiness_plan_completed_with_warnings_ready_for_controlled_tool_call_readiness_proof` as source evidence. Before any mutation or execution, re-read the current repo state and stop if a duplicate/superseding PR exists, source lineage drifted, disk is unsafe, or any media/artifact/Supabase/provider/worker/route scope appears.

This future prompt may only attempt a controlled local synthetic readiness proof for the static allowlist. It must not open media files, run FFmpeg/ffprobe, process media, create artifacts, call providers/models, dispatch workers, execute routes, mutate Supabase, run SQL, create signed/public URLs, mutate billing/credits/Stripe, unlock beta, unlock production, run Docker/GCP/Cloud Run, or claim runtime/beta/production readiness.

Required proof boundaries: synthetic in-memory inputs only, fail-closed runtime flags, no media paths, no artifact targets, sanitized outputs only, package-lock unchanged, temp files outside tracked source, and immediate stop on any widened scope. This prompt must still report that tool-call execution is a controlled proof only, not production readiness or beta readiness.

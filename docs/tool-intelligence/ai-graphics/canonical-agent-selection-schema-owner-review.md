# Canonical Agent Selection Schema Owner Review

Owner accepted: true

Required schema sections accepted: `capabilityExtraction`, `candidateMapping`, `ranking`, `elimination`, `fallback`, `missingProofRequirements`, `planningOnlyRecommendation`, `safetyBoundary`.

The schema remains planning-only. Agent, tool, route, worker, browser/WebGL/canvas, GPU/model, storage, signed URL, public artifact, runtime, beta, and production execution stay false.

Citations: PR #674, PR #671, PR #668, PR #623.

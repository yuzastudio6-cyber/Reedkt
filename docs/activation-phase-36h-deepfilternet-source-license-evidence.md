# Phase 36H DeepFilterNet Source And License Evidence

Official sources refreshed for Phase 36H:

- DeepFilterNet GitHub: `https://github.com/Rikorose/DeepFilterNet`
- DeepFilterNet PyPI: `https://pypi.org/project/deepfilternet/`

DeepFilterNet is recorded as the active speech-cleanup path from Phase 36G. Phase 36H uses this public evidence only as source/license/runtime metadata; it does not replace the approved private Phase 36B/36C runtime artifacts.

The selected runtime artifacts remain:

- CLI: `deep-filter-0.5.6-x86_64-unknown-linux-musl`
- Model archive: `DeepFilterNet3_onnx.tar.gz`
- Aggregate SHA-256: `eab42c424fc818938f1b8591f4110318f86284b520e5244e571c2f3f56f5126b`

PyPI runtime fallback is not approved for Phase 36H unless a future phase explicitly approves it. Runtime package fallback must block if it would trigger model auto-download, unverified models, network-at-runtime behavior, or repo dependency changes.

# Build Context Command Review

The CPU-worker Dockerfile requires `dist-server` and `dist-staging-fixture-worker`.
Only `npm run build:server` and `npm run build:staging-fixture-worker` were approved for context generation.

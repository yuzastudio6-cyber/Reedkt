# Project Edit Brief Conflict Detection

RP-EDITBRIEF-10 detects mock/local marker conflicts from metadata only. The deterministic checks cover cut-vs-B-roll overlap, cut-vs-keep overlap, do-not-use-vs-action overlap, music-vs-no-music, SFX-vs-no-fake-sounds, invalid time ranges, and exact reference copy risk.

Conflict records are saved only through the existing mock Project Edit Brief conflict client route where safe. They are review metadata, not planner instructions. The UI shows conflict count, related marker context, and recommended resolution text, but it does not auto-fix conflicts.

There is no Qwen, no DeepSeek, no providers, no workers, no render, no credits, no Supabase command, no media processing, no file-byte read, no external URL fetch, no sound runtime, and no planner application.

owner review remains pending before conflict-free markers can become future plan inputs.

Marker QA boundary summary: mock/local, no Qwen, no DeepSeek, no providers, no workers, no render, no credits, no Supabase command, no planner application, owner review pending.

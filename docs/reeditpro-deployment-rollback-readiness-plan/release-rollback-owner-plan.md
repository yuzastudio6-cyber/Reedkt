# Release And Rollback Owner Plan

Required owner slots before any deployment execution:

- Release owner: `release_ops_owner_required_before_external_beta`.
- Rollback owner: `rollback_ops_owner_required_before_external_beta`.
- Incident commander: `incident_owner_required_before_external_beta`.
- Support escalation path: required before external beta.

These are owner requirements only. No deployment or rollback command is authorized here.

# Font Config Attempt Report

Runtime proof with the env var passed first; the Dockerfile then received the same minimal env var for durable OCR runtime configuration.
No package source file, requirements file, lockfile, `.dockerignore`, font asset, or model asset was modified.

# Runtime Boundary

This gate is metadata only. It does not dispatch workers, call providers, process media, render/export, create artifacts, write Supabase/GCS, run SQL, mutate billing/credits, run Docker, expose external beta, or unlock production.

# Milestone 1 Approval

Approved future prompt: `TRACKB_MEDIA_OSS_MILESTONE_1_LOW_RISK_METADATA_TOOLING_EXECUTION`

Milestone 1 is approved as future execution only. This phase does not install tools or run proofs.

| Tool | Compute | Future proof |
| --- | --- | --- |
| ExifTool | CPU only | Version proof and tiny synthetic metadata fixture |
| MediaInfo | CPU only | Version proof and tiny synthetic media-header fixture if safely available |
| Tesseract | CPU only | Version proof and tiny synthetic image OCR fixture |
| ImageMagick / GraphicsMagick | CPU default | ImageMagick preferred; version proof and tiny synthetic image transform fixture |

Milestone 1 must use synthetic tiny fixtures only, no real user media, no public artifacts, and no signed URLs.

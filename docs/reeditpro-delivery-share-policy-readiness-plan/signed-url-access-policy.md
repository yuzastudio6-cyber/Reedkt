# Signed URL Access Policy

Future signed URLs must be short-lived, revocable, audit logged, project scoped, and tied to authenticated user or support context. They must not be permanent public delivery links.

This plan defines the policy only. It does not create URLs, buckets, storage objects, secrets, routes, or Supabase/GCS writes.

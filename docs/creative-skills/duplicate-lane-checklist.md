# Creative Skill System Duplicate-Lane Checklist

Future Creative Skill System prompts must run this checklist before adding new docs, contracts, UI, runtime code, migrations, provider logic, or worker behavior.

| Check | How to check | Current result | Risk level | Next action |
| --- | --- | --- | --- | --- |
| Duplicate Creative Skill architecture docs | `rg -i "creative skill|creative skills|skill system|skill contract|editing_skill|skill_id"` | No direct matches before this audit directory was added. | Low | Keep new skill docs under `docs/creative-skills/` until doctrine is stable. |
| Duplicate planning contract docs | Inspect `intent-led-edit-planning.md`, `type-contracts.md`, `ai-editor-data-model.md`, `connected-planning-system-overview.md`, and `planning-layer-inventory.md`. | Planning contracts and layers already exist across docs and types. | High | Reference existing planning records instead of inventing parallel skill plan records. |
| Duplicate prompt architecture | Inspect `provider-prompt-architecture.md`, `generation-provider-architecture.md`, prompt builders, and provider-related types. | Approved plan to provider prompt flow already exists. | High | Skills may describe prompt intent later, but provider prompts remain downstream of approved plans. |
| Duplicate tool registry | Inspect `open-source-tool-registry.md`, `src/lib/tool-registry.ts`, `server/tool-registry/*`, and `docs/tool-calling/*`. | Tool candidates, capability cards, policies, and production profiles already exist. | High | Skills should reference tool capability records, not create a second tool registry. |
| Duplicate TypeScript contracts | Inspect `src/types/index.ts`, `src/types/planning.ts`, `src/types/signature-systems.ts`, `src/types/edit-quality.ts`, `src/types/audio-music.ts`, `src/types/generation.ts`, `src/types/jobs.ts`, and `src/types/reeditpro.ts`. | Broad contract surface already exists. | High | Do not add skill types until doctrine identifies exactly which existing records need extension. |
| Duplicate Supabase migrations | Inspect `supabase/migrations/*`. | Migrations already cover planning, credits, approval, jobs, generated assets, render QA, SFX, StoryTiming, worker leases, and E2E runtime readiness. | High | No Creative Skill migration in `RP-SKILLS-00` or doctrine-only follow-ups. |
| Duplicate mock planner behavior | Inspect `src/lib/mock-planner.ts`, `src/lib/intent-compiler.ts`, `src/lib/planner-validation.ts`, and planning UI cards. | Mock planner already creates source sequence, compiled intent, signature routes, credits, QA, timing, and approval artifacts. | Medium | Future skills should first document how they map into planner inputs and outputs. |
| Duplicate chat UI lane | Inspect `src/components/editor/*` and `chat-planning-ux-architecture.md`. | Chat-native planning cards already cover many plan concepts. | Medium | Do not add skill UI until card priority, collapsed states, and approval path are defined. |
| Duplicate signature routing | Inspect `signature-systems.md`, `visual-storytelling-architecture.md`, `src/types/signature-systems.ts`, and `src/lib/mock-planner.ts`. | Stroke Motion, Graphic Design, Real Motion, and SoundSync routing already exist as signature decisions. | High | Skills must not force signature systems or bypass story/intent/credit/QA routing. |
| Duplicate SoundSync/SFX/music lane | Inspect `soundsync-audio-pipeline-planning.md`, `docs/chat-native-music-ui.md`, `docs/chat-native-sfx-ui.md`, `docs/production-soundsync-*`, `src/types/audio-music.ts`, and `src/types/sfx-director.ts`. | Extensive audio planning and worker boundary docs exist. | High | Reconcile with SoundSync and SOUND CPU PRs before adding audio skill contracts. |
| Duplicate worker schema or runtime lane | Inspect `job-orchestration-architecture.md`, `worker-tool-runtime-architecture.md`, `docs/production-worker-*`, `src/types/jobs.ts`, `src/types/job-runtime.ts`, and `src/types/worker-lease.ts`. | Worker orchestration, leases, runtime, gates, and policies already exist. | High | Skills can request planned work later; they must not directly own worker runtime. |
| Duplicate provider selection lane | Inspect `generation-provider-architecture.md`, `provider-prompt-architecture.md`, `src/types/generation.ts`, and provider migrations. | Provider policy and generated asset records already exist. | High | Skills must stay upstream of provider selection and model-specific execution. |
| Duplicate 3D/browser tool lane | Inspect `open-source-tool-registry.md`, `docs/tool-calling/studies/three_js.json`, `babylon_js.json`, `cesium_js.json`, `playwright.json`, and `server/tool-registry/*`. | 3D and browser/app visuals are represented as tool candidates and future runtime profiles. | Medium | Define future 3D/browser skills as planning modes only until tool runtime is approved. |
| Duplicate open PR streams | Use `gh pr list --repo yuzastudio6-cyber/Reedkt --state open --search ...`. | At least 200 open PRs; 147 matched skill/tool/graphics/SoundSync terms on June 24, 2026. | High | Reconcile with active PR clusters before converting skill docs into contracts or runtime. |

## Required Result For Future Prompts

A future prompt should be allowed to add new skill artifacts only when it can state which existing source truth it extends and which duplicate lane it avoids. If the answer is unclear, the prompt should remain docs-only.

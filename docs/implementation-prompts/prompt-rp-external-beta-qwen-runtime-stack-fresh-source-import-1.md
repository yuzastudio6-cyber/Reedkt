# RP-EXTERNAL-BETA-QWEN-RUNTIME-STACK-FRESH-SOURCE-IMPORT-1

Use after `RP-EXTERNAL-BETA-SINGLE-TESTER-FEEDBACK-DRIVEN-FIX-LOOP-1` records `fresh_source_import_required_no_blind_stack_merge`.

## Goal

Create a fresh current-integration source import for the QWEN2.5-VL external-beta runtime stack evidence without blindly merging the open stacked branch chain.

## Required Carry-Forward

- Current tester lane: `go_single_tester_only`.
- Active tester: `aiediting@reeditpro.com`.
- Main ReEditPro Supabase project: `Reeditpro` / `wmyyttnynmteqgcdishd` / `staging`.
- Source-derived owner decisions are accepted when repo/GitHub/current-thread evidence is sufficient.
- The open QWEN stack is evidence, not direct merge approval.
- Additional tester expansion remains `blocked_no_additional_named_tester_list`.
- Broad external beta, public artifacts, signed URLs as source-of-truth, paid billing, final delivery/export, broad media, and production remain blocked.

## Boundary

Do not run provider/model calls, Cloud Run invocations, Supabase/SQL mutation, workers, media processing, signed/public artifacts, package install, or deployments unless a later packet explicitly names the target, confirmation gate, cost boundary, secret boundary, rollback path, and validation.

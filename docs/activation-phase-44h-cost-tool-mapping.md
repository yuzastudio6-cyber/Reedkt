# Phase 44H Cost Tool Mapping

Phase 44H maps exactly 18 Track B tool ids from the capability and route manifests.

CPU worker planning classes are assigned to DeepFilterNet, Signalsmith Stretch, PaddleOCR, OpenCV, PyAV, and PySceneDetect. Sharp/libvips, DuckDB, Polars, web/desktop profilers, desktop benchmark runner, and route manifest integration remain metadata/local/light planning classes.

PaddlePaddle is dependency/handoff-only. Demucs is blocked pending provenance/model approval. Qwen3-VL and vLLM are GPU/VLM blocked and excluded. Local worker sidecar planning remains not started.

Mappings do not make any route executable.

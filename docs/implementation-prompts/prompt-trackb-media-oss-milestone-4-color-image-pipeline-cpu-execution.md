# TRACKB_MEDIA_OSS_MILESTONE_4_COLOR_IMAGE_PIPELINE_CPU_EXECUTION

Implement the future CPU-only Track B bounded install/proof execution for OpenColorIO and OpenImageIO. Use the approved target `docker/prod/cpu-worker/Dockerfile` with `docker/prod/cpu-worker/requirements.cpu.txt`. Pin package versions, record package source/license metadata, use only tiny synthetic temporary fixtures, and keep real media, render/export, GPU, Supabase/GCS, public artifacts, signed URLs, beta, and production blocked.

# apps/web/public

This directory is a Phase 44B scaffold only.

The active public assets remain in root `public` so current Vite behavior stays
unchanged. Future asset movement should keep browser assets separate from
private artifacts, model files, secrets, and generated media.

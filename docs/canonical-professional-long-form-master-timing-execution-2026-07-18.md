# Canonical Professional Long-Form Master-Timing Execution — 2026-07-18

Status: `superseded_by_first_object_chunk_execution`

The master-timing milestone described below remains historical evidence. The
current continuation also completes the first 4K object render and its paired
independent QA, leaving 250 children blocked; see
`docs/canonical-professional-long-form-first-object-chunk-execution-2026-07-18.md`.

## Outcome

The specialized 255-job professional long-form queue now executes its third
bounded validation child: `long-form-validate-master-timing`. The child runs
only after the exact approved snapshot root is terminal. It receives one
server-persisted authorization, one private QA-worker lease, and one one-use
attempt. It validates immutable structured timing authority, persists a
content-addressed validation artifact, QA evidence, downstream reconciliation,
attempt-level internal production-cost evidence, and terminal evidence, then
commits one terminal queue completion. Restart returns the exact stored result
without another attempt.

This is structured approved-metadata validation. The runner does not read media
bytes, align a transcript, detect beats, decode or transform audio/video, call a
provider, render, dispatch to Google Cloud, or create a public deliverable.

## Canonical Timing Profile

The focused long-form fixture now publishes
`canonical_professional_long_form_source_led_no_generated_cues_v1` instead of
the former generic timing objects. The profile freezes:

- the exact rational frame rate and frame execution unit;
- first frame `0`, the exact exclusive final frame, and total frame count;
- one ordered segment set with unique identities and gap-free, overlap-free,
  full-duration coverage;
- the exact approved hard-cut count;
- the approved source-cleanup hash and meaning-preservation requirement;
- the priority order speech clarity, story meaning/emotion, visual
  readability, music/beat, motion smoothness, platform retention, then
  decorative effects;
- renderer use of approved frame values only and no required placeholder in a
  final master;
- zero planned captions, generated visual cues, music, SFX, ducking, or beat
  grid for this source-led fixture; and
- approved-snapshot-only worker behavior with a new approval required for a
  material timing change.

The no-generated-cue profile is intentionally narrow. It does not claim that a
future caption-, music-, SFX-, transcript-, provider-, or beat-heavy edit can
skip its corresponding analysis and timing evidence.

## Validation Categories

The immutable timing-validation component contains the complete 18-category
catalog:

1. frame confirmation;
2. timing base;
3. source timing;
4. final timeline;
5. transcript timing;
6. caption readability;
7. visual readability;
8. caption/visual collision;
9. transition safety;
10. beat alignment;
11. SFX justification;
12. music ducking;
13. provider clip duration;
14. Remotion layer timing;
15. tier complexity;
16. credit impact;
17. approval gate; and
18. worker readiness.

Applicable source-led categories pass. Categories for work that is explicitly
not planned pass as not applicable; they are not silently omitted.

## Approval and Estimate Boundary

The timing child reconstructs the approved timing hash from the exact five
content-addressed component references. It separately reopens the approved
`long-form-object-graph` estimate-line metadata and requires:

- child execution is included in the approved reservation;
- timing complexity is included in the approved estimate;
- the exact timing profile identity; and
- the `simple` complexity classification used by this fixture.

The attempt records provisional local internal infrastructure cost under the
versioned
`reeditpro_long_form_master_timing_validation_cpu_2vcpu_4gib_v1` profile. This
cost is internal production-cost evidence only. It is not a customer price,
customer credit charge, ReEditPro service fee, wallet mutation, billing
instruction, or settlement record. No second export estimate or charge is
created.

## Dependency Enforcement

Every object-mezzanine render now depends on all three applicable authorities:
the approved-snapshot root, private-source authority, and verified master
timing. Continuous program audio depends on source authority and master timing.
The private 4K finalizer retains its direct master-timing dependency in addition
to all chunk QA, continuous audio, and cross-chunk color continuity.

The completed timing reconciliation records exactly:

- 124 render dependents;
- one continuous-audio dependent; and
- one private-finalization dependent.

All 126 remain unexecuted and capability-blocked. The focused queue therefore
ends with three completed validation children and 252 queued children.

## Queue and Integrity Hardening

Professional long-form queue definitions now preserve each child manifest's
exact expected-output identity. An authorization receipt must match the queue
job, work item, definition hash, placement hash, expected output, worker type,
resource class, attempt limit, timeout, canonical order, and completed root
dependency. Timing uses `qa_worker`, `qa_cpu_standard_v1`, one attempt, and a
900-second attempt ceiling.

Focused adversarial coverage proves:

- an expired reservation creates no queue authorization event or attempt;
- concurrent service calls consume exactly one lease and one attempt;
- a fabricated expected-output identity is rejected;
- restart reopens exact authority, artifacts, cost, and completion;
- root and source evidence remain immutable after timing queue progress;
- render, audio, and finalization claims remain refused without mutation;
- artifact, QA, reconciliation, terminal, and commercial-boundary tampering is
  rejected;
- checksum-corrupted persisted timing evidence fails after process-state reset
  and exact restored bytes replay;
- approved master-timing component, segment, and estimate-metadata blob
  corruption fail closed; and
- timing authority sources import no cloud, database, provider, network,
  environment, or child-process activation path.

## Focused Verification

The following commands pass on the exact working tree:

- `npm run typecheck:server`;
- `npm run smoke:professional-long-form-object-plan` — 27 checks;
- `npm run smoke:professional-long-form-snapshot-bridge` — 22 checks;
- `npm run smoke:canonical-private-package-work-queue` — 11 checks;
- `npm run smoke:private-internal-attempt-cost-evidence` — includes the new
  fixed 2-vCPU/4-GiB/CPU-only timing profile; and
- `npm run smoke:canonical-professional-long-form-post-approval` — 88 checks in
  the current continuation, including the later first render/QA pair.

The current exact-code `npm run qa:internal-pipeline` v23 aggregate passed all
`38/38` stages with exit code `0`. It started at
`2026-07-18T15:34:20.145Z`, finished at `2026-07-18T16:14:33.676Z`, and
completed in `2,413,531 ms`. The object-plan stage passed 27 checks in `371 ms`,
the approved-snapshot bridge passed 22 checks in `789 ms`, the isolated
deterministic object runner passed in `21,735 ms`, and the 88-check canonical
post-approval lifecycle passed in `85,596 ms`. The prior v22 run remains the
exact historical aggregate for the master-timing-only milestone.

The same run reverified exactly 50 private canonical tool lifecycles and 50 job
adapters, the crash-consistent package lifecycle, bounded three-source media
composition, professional color execution, above-16-MiB UHD Remotion streaming,
all 11 named-edit browser tests, and the accepted maximum-eight-source private
review. The current report keeps all 250 remaining long-form children, complete
source-media/chunk execution, object storage, distributed database activation,
live Google Cloud, customer billing, public delivery, external beta, and paid
production false.

## Readiness Truth

Now verified locally/private for this bounded child:

- exact approved timing-component and expected-output identity;
- exact rational frame base and complete structured segment coverage;
- source-cleanup, timing-priority, transition, no-random-cue, approval, and
  estimate metadata boundaries;
- server-persisted authorization and content-addressed evidence;
- one QA-worker lease and one-use attempt;
- private QA, reconciliation, internal attempt cost, terminal completion, and
  restart replay; and
- 126 downstream timing-dependency records without downstream execution.

Still false:

- transcript or audio alignment;
- beat detection, caption rendering, visual-cue execution, music, SFX, or
  ducking analysis;
- source-media decode or transformation in this child;
- at this milestone, the remaining 252 child operation authorities, leases,
  attempts, and costs; the later first render/QA pair reduces the current
  blocked count to 250;
- chunk render, continuous audio, cross-chunk color, finalization, and final QA;
- long-form object-store media persistence;
- distributed database authority;
- live Google Cloud worker execution;
- provider activation, customer billing, wallet mutation, or public delivery;
- external-beta readiness; and
- production readiness.

## Next Evidence Gate

The representative first object-mezzanine chunk operation is now complete and
documented in the continuation linked above. The next dependency-safe slice is
separate continuous program audio or a bounded multi-chunk continuity proof.
The one completed chunk still does not authorize all 124 chunk renders or
imply that continuous audio, color continuity, final assembly, Google Cloud,
or the full product pipeline is ready.

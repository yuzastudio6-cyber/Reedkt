# AI Graphics Job Payload Invalid Dry-Run Gate Status QA

Decision: `worker_ai_graphics_metadata_job_payload_dry_run_gate_status_qa_passed_with_warnings`

The invalid dry-run case gate status from PR #509 is accepted with warnings. QA confirms invalid cases remain fail-closed and reject missing placeholder refs, unsafe artifact scope, generic pass claims, true runtime booleans, or executable instructions.

Status: `accepted_with_warnings`.

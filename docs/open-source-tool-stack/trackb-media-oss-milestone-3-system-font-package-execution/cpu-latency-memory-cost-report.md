# CPU Latency Memory Cost Report

Docker build elapsed time before failure: `61` seconds.
Font discovery, PaddlePaddle, and PaddleOCR timings are not available because the image was not built.
GPU was not used. `fonts-noto-cjk-extra` remains unnecessary and fallback-only.

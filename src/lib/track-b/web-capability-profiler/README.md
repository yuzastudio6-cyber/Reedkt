# Web Capability Profiler

This browser-safe Track B module returns coarse, session-scoped capability metadata for future route planning. It does not upload profiles, persist identifiers, call providers, run benchmarks, process media, execute routes, or import server code.

The live collector returns a local object only. Generated fixtures are the only execution evidence for Phase 44D.

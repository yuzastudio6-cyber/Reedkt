# Package Install Proof Report

Installed package proof: `gpac	26.02-rev0-g118e60a90-HEAD	arm64`.

Install source verified: `https://dist.gpac.io/gpac/linux/debian bookworm/main`.

The Dockerfile simulated `gpac=26.02-rev0-g118e60a90-HEAD` before real install. The simulation included Debian-sourced dependencies and `gpac` from `dist.gpac.io`; no simulated non-`gpac` package from `dist.gpac.io` was accepted.

This proof is package/install-source evidence only. It does not approve GPAC/MP4Box runtime behavior, media processing, product runtime use, beta, or production.

# TOOL-ROUTE-2 Owner Fixture Handoff Map

- `WEB_SEARCH_CAPTURE`: fixtures `web_search_capture_fixture`; review only `true`; execution authorized `false`.
- `MAP_GEOSPATIAL`: fixtures `map_geospatial_fixture`; review only `true`; execution authorized `false`.
- `AI_TOOLS_CREATIVE_GRAPHICS`: fixtures `ai_tools_creative_graphics_fixture`; review only `true`; execution authorized `false`.
- `TRACK_A_RENDER_EXPORT`: fixtures `track_a_render_export_fixture`; review only `true`; execution authorized `false`.
- `TRACK_B_MEDIA_PROCESSING`: fixtures `track_b_media_processing_fixture`; review only `true`; execution authorized `false`.
- `SOUND_MUSIC_AUDIO`: fixtures `sound_music_audio_fixture`; review only `true`; execution authorized `false`.
- `WORKER_RUNTIME_JOBS`: fixtures `worker_runtime_job_planning_fixture`; review only `true`; execution authorized `false`.
- `PROVIDER_GATEWAY_MODELS`: fixtures `provider_model_planning_fixture`; review only `true`; execution authorized `false`.
- `SUPABASE_RLS_STORAGE_DATABASE`: fixtures `supabase_metadata_storage_fixture`; review only `true`; execution authorized `false`.
- `OBSERVABILITY_AUDIT_COST`: fixtures `observability_audit_cost_fixture`; review only `true`; execution authorized `false`.
- `COMPLIANCE_SECURITY`: fixtures `compliance_security_fixture, public_artifact_signed_url_delivery_blocked_fixture`; review only `true`; execution authorized `false`.
- `FRONTEND_PRODUCT_UX`: fixtures `frontend_product_ux_fixture`; review only `true`; execution authorized `false`.
- `BILLING_STRIPE_CREDITS`: fixtures `billing_stripe_credits_fixture`; review only `true`; execution authorized `false`.

No Supabase mutation, SQL execution, Google Cloud API call, Secret Manager API call, provider call, model call, tool execution, worker execution, route execution, browser capture, Docker/Cloud Run execution, storage transfer, signed URL creation, public artifact creation, credit mutation, Stripe checkout/webhook/payment processing, deployment, internal beta unlock, external beta unlock, production unlock, dependency mutation, raw prompt execution, final render/export, or broad service-role handler was enabled.

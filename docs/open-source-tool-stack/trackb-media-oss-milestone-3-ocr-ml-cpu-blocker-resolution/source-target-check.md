# Source Target Check

OCR runtime source target is present and uses committed repo files only.
No generated build context is required for this OCR runtime Dockerfile.
Requirements and lockfile remain out of scope.

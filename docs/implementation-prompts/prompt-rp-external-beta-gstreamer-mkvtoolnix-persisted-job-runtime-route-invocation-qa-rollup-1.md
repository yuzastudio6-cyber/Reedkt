# RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-PERSISTED-JOB-RUNTIME-ROUTE-INVOCATION-QA-ROLLUP-1

Use the merged `RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-PERSISTED-JOB-RUNTIME-ROUTE-INVOCATION-1` route.

Goal: invoke `/v1/external-beta/gstreamer-mkvtoolnix/narrow-agent/generated-fixture-runtime/approved-snapshot/jobs/persisted-invoke` once with an approved generated-fixture persisted job payload and the explicit confirmation gates, then record sanitized `/tmp` report and manifest evidence.

Required boundaries: generated fixture only, no private/user media, no arbitrary file path, no public URL, no signed URL source-of-truth, no provider/model calls, no FFmpeg/FFprobe, no final render/export, no paid production unlock, no broad service-role handler, and no generated artifacts committed.

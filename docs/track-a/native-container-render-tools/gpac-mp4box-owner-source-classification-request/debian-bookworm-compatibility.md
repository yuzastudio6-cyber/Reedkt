# Debian Bookworm Compatibility

Current render-worker base: `node:24-bookworm`.

Selected future repository URI for approval review: `https://dist.gpac.io/gpac/linux/debian`.

Selected codename: `bookworm`.

Selected component: `main`.

Blocked component: `nightly`.

The official GPAC Debian repository lists `bookworm` and includes `amd64` and `arm64`, which are the future review targets. Native Debian package paths remain not selected because prior Debian source review did not prove a clean current bookworm native `gpac` source class, and Debian sid remains an unsafe stable-bookworm source substitute.

Classification: `official_gpac_apt_repository_ready_for_approval_review`.

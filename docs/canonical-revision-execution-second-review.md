# Canonical Revision Execution And Second Review

Status: private single-host revision execution and accepted second-review evidence

The canonical backend now executes an approved replacement snapshot through the bounded professional work graph and assembles a new private review with its own final artifact, independent final QA, manifest, and user decision.

## Revision execution chain

After exact revision handoff, plan-v2 publication, fresh estimate, and atomic synthetic approval reconciliation, the backend:

1. derives a new execution package from snapshot v2;
2. executes approved-snapshot validation;
3. validates the approved source-trim decisions;
4. generates the approved libass caption overlay;
5. renders the trim-authoritative Remotion H.264/AAC composition;
6. independently probes that exact final dependency with FFprobe;
7. requires private artifact persistence, QA, reconciliation, lease binding, and replay for every required output;
8. assembles a new credential-free private-review manifest;
9. records one create-only private-internal acceptance decision.

The second review uses new package, snapshot, job, artifact, final-QA, assembly, and decision identities. It cannot silently reuse plan v1's terminal authority.

## Current executable evidence

The canonical lifecycle smoke proves:

- snapshot v2 produces exactly five canonical jobs;
- all five complete in dependency order with zero required blockers;
- work-graph replay does not execute tools again;
- the second final artifact and final-QA artifact identities differ from plan v1;
- independent final QA is bound to the exact second final MP4;
- the second private review has its own create-only manifest;
- private-internal acceptance has no revision handoff;
- acceptance replay returns the same decision;
- public delivery, billing, settlement, and production remain false;
- the complete smoke contains 61 one-use dispatch grants, including both bounded review passes.

## Boundaries

Private-internal acceptance does not authorize public export or product delivery. Superseded plan-v1 artifacts are no longer readable through the active-execution download path after plan-v2 approval; the dedicated authenticated history/recovery reader now reopens them without restoring execution authority.

Deployed real-user upload/browser identity, deployed storage/workers, Supabase/RLS, providers, billing, public delivery, external beta, and production remain gated. The reviewed local/private named-edit browser now handles playback, exact decisions, and immutable history separately from this execution proof.

## Verification

- `npm run typecheck:server`
- `npm run lint`
- `npm run smoke:canonical-private-tool-dispatch`
- `npm run smoke:proven-tool-identities`
- `npm run qa:internal-pipeline`
- `npm run check:frontend-boundary`

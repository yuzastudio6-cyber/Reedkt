# Future Verification Command Plan

- Future commands are bounded import/version/API-shape checks only.
- OpenColorIO may use raw/minimal config object checks; OpenImageIO may use ImageSpec or tiny temp synthetic fixture checks.
- Real media, render/export, public artifacts, signed URLs, and GCS remain blocked.

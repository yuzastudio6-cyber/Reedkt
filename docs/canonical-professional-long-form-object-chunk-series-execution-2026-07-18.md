# Canonical Professional Long-Form Object-Chunk Series Execution — 2026-07-18

Status: `generic_executor_verified_two_of_124_pairs_completed_local_private`

## Outcome

One server-owned executor now covers all 124 render/QA pairs in the immutable
255-job six-hour graph. The caller supplies only workspace and approved
snapshot identity. The server selects the first incomplete pair, derives its
canonical order, reopens the approved snapshot, funded 4K reservation, source
authority, Master Timing, package, placement, and queue, and rejects caller
chunk/source/path/command/recipe/artifact authority.

Retained evidence completes two representative pairs plus the three
prerequisites and continuous program audio: 8/255 jobs completed and 247 still
queued. This proves a generic executable class; it does not claim that the
remaining 122 pairs have been run.

## Media Contract

- 124 approved chunks, canonical render orders 3–249 (odd), and paired QA
  orders 4–250 (even).
- One through eight immutable H.264 MP4 sources and one through 16 exact
  source slices per chunk.
- This retained local/private runner is intentionally bounded to 192 MiB per
  staged source and 512 MiB combined input/output per chunk. Those are test
  safety ceilings, not the future product upload or project-size limits.
- Source staging follows immutable approved-source order; timeline assembly
  independently follows approved slice order, including revisiting a prior
  camera/source.
- Nonzero source-frame decode/trim, approved hard cuts, and
  `continuous_technical_split` boundaries are supported.
- Every slice is normalized to the confirmed 4K frame, 30 fps, `yuv420p`, and
  BT.709 limited range, then encoded as private VP9 CQ12 Matroska.
- Audio remains in the separately verified continuous lossless FLAC authority.
- Public H.264 encoding/export remains blocked and is not compiled into this
  LGPL image.

## Canonical Lifecycle

Every selected render and QA retains exact authority persistence, queue
authorization, lease, one-use dispatch, attempt identity, create-only private
artifact persistence, independent FFprobe QA, attempt-level internal
production cost, reconciliation, terminal completion, and replay/tamper
boundaries. Internal production cost stays separate from customer price,
credits, service fee, wallet, and billing authority. The original approved 4K
estimate/reservation is reused without a second export estimate or charge.

Earlier evidence remains immutable as later pairs advance. The last pair may
make cross-chunk color dependencies ready, but does not self-authorize color,
finalization, or final QA.

## Executed Proof

- Pinned image safety verification passed for
  `reeditpro/ffmpeg-lgpl-internal:8.1.2-object-chunk-v5-local`, image ID
  `sha256:7b4d3344569eaee7dfcf13d9aaae2be778ab178fcf2e227bb7bfc27472047673`.
- `npm run smoke:offline-media-binary-object-chunk` passed a 1,350-frame
  timeline-start chunk, byte-deterministic replay, independent VP9 QA, and a
  later chunk with source start frame 225 and a technical-split boundary.
- `npm run smoke:canonical-professional-long-form-post-approval` passed all 99
  checks on the six-hour/512-range/124-chunk/255-job fixture.
- Canonical chunk 1 and server-selected chunk 2 each produced a real private
  4K VP9 artifact and independent exact-frame/color QA evidence. Chunk 2 began
  after global frame zero and its first slice used a nonzero source frame with
  `continuous_technical_split`.
- TypeScript server type checking passed.

## Readiness Truth

Verified local/private:

- one generic backend executor and queue verifier for all 124 pair identities;
- bounded proof of two different pairs, including the later-chunk cases that
  invalidated the old first-only stream-copy shortcut;
- exact approved-source staging, slice mapping, private persistence, QA, cost,
  reconciliation, and queue completion; and
- 8/255 retained completions with honest remaining counts.

Still false:

- execution proof for the other 122 render/QA pairs;
- retained six-hour cross-chunk color-continuity execution (a separate bounded
  two-chunk canonical proof now passes; see
  `docs/canonical-professional-long-form-cross-chunk-color-continuity-2026-07-18.md`);
- private master assembly and final-master QA;
- production codecs, representative multi-camera editorial quality, and
  throughput/load evidence;
- distributed database/object-store recovery and multi-host concurrency;
- live Google Cloud dispatch/workers, deployment, or observability;
- provider activation, billing, wallet mutation, customer charging, public
  delivery, external beta, or production readiness.

## Next Gate

The next unique capability is canonical private-master assembly. It must
consume only independently QA-passed chunk artifacts, continuous program
audio, and passed color evidence while preserving the same snapshot,
reservation, private persistence, one-use execution, cost, reconciliation,
and replay boundaries. Full assembly in the retained six-hour graph remains
blocked until every chunk pair and color dependency passes.

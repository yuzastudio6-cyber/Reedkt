# Canonical Agent Selection Fallback Rules QA

Decision: `ai_graphics_canonical_agent_selection_qa_passed_with_warnings`.

Fallback rules include preferred, conditional, and fallback planning tools where applicable. No fallback authorizes execution.

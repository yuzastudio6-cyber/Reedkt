# AI Graphics Job Payload Claim Lease Placeholders

Decision: `worker_ai_graphics_metadata_job_payload_shape_approved_with_warnings`

Future payloads must include `workerJobRef`, `claimPlaceholderRef`, `leasePlaceholderRef`, `idempotencyKeyRef`, and `retryPolicyRef` as placeholders only.

This packet does not approve worker execution, real job claim, lease mutation, retry execution, or broad service-role job mutation.

# Tool Package Strategy

- Future execution must review pinned Python package names, versions, sources, and licenses for OpenColorIO/OpenImageIO.
- Future target requirements file is `docker/prod/cpu-worker/requirements.cpu.txt` if patching is approved later.
- This approval phase mutates no requirements, Dockerfiles, package-lock, or dependency files.

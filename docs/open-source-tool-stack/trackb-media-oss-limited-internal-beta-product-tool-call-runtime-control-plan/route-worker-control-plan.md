# Route And Worker Control Plan

Routes and worker dispatch remain disabled by default. Future approval must prove explicit approved snapshot, credit, idempotency, contract, per-tool allowlist, kill switch, schema validation, QA fallback, rollback, and sanitized logging controls.

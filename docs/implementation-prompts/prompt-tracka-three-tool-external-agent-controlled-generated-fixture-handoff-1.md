# TRACKA-THREE-TOOL-EXTERNAL-AGENT-CONTROLLED-GENERATED-FIXTURE-HANDOFF-1

Use `TRACKA-THREE-TOOL-EXTERNAL-AGENT-RUNTIME-READY-ROLLUP-1` as the source-of-truth for external-agent handoff planning across:

- `gstreamer_render_pipeline_support`
- `mkvtoolnix_container_validation`
- `gpac_mp4box_packaging_validation`

The next packet may define the agent handoff envelope for controlled generated-fixture execution only. It must preserve route gates, generated-fixture-only input classes, `/tmp` evidence handling, artifact checksums, no public artifacts, no signed URLs, no private/user media, no Supabase mutation, no SQL execution, no FFmpeg/FFprobe expansion, and no production/final-render unlock.

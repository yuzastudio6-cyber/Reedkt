# Canonical Agent Selection Next Lane

Decision: `ai_graphics_canonical_agent_selection_review_passed_with_warnings`.

Recommended next prompt: `AI_GRAPHICS_CANONICAL_AGENT_SELECTION_QA_REVIEW`.

This recommendation does not unlock runtime, route, worker, provider/model, storage, public artifact, beta, or production execution.

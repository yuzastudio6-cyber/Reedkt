# Canonical Agent Selection Missing-Proof Rules Canonicalization Owner Approval QA

Decision: `ai_graphics_canonical_agent_selection_canonicalization_owner_approval_qa_passed_with_warnings`

QA-accepts missing-proof gates before any future execution.

- Missing CPU import, model provenance, browser runtime, GPU/model-weight, Tool Route, Worker, storage, signed URL, or public artifact proof blocks execution.
- Agent can still select tools for planning/study metadata when capability fit is useful.

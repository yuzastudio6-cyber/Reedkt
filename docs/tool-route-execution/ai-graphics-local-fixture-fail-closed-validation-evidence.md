# AI Graphics Local Fixture Fail-Closed Validation Evidence

Decision: `tool_route_ai_graphics_metadata_local_fixture_validation_passed_with_warnings`

Fail-closed validation passed with warnings. Missing required placeholder fields, unsafe runtime requests, public artifact requests, signed URL requests, raw prompt execution requests, provider raw output requests, real user data requests, and unscoped artifact paths must block the fixture validation lane.

The validation result does not approve local fixture execution, route execution, actual tool execution, worker execution, provider/model runtime, browser/WebGL/canvas runtime, Supabase mutation, GCS upload, beta, or production.

# Fixture Execution Scope

The next lane may plan a controlled internal beta fixture execution using synthetic or separately approved private fixtures only. It must preserve approved snapshot, credit, private artifact, QA, fallback, idempotency, result-schema, and sanitized logging gates.

User media, public artifacts, signed URLs, live beta runtime, production, and product-ready claims remain blocked unless a later approved gate explicitly proves them.

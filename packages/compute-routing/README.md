# packages/compute-routing

`packages/compute-routing` is reserved for future compute routing policy.

## Owns Later

- `ToolRouteManifest`
- `DeviceComputeProfile`
- `WebCapabilityProfile`
- compute decision policy

## Phase 44A Status

This package is planned only. It must not run local hardware scans, route active jobs to a local worker, execute local AI, invoke providers, or bypass server readiness gates.

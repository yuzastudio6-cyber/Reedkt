# CPU Static Spec Validation Execution: vega_lite

Decision: `blocked_pending_cpu_static_dependency_install_from_lock`

Tool: `vega_lite`

Package: `vega-lite`

Canonical proof level preserved: `canonical_merged_package_import_static_fixture_proof`

The approved future fixture was an in-memory minimal Vega-Lite spec compile/validation metadata check with no browser render or export. The execution did not run because `vega-lite` is absent from `package.json` and `package-lock.json`.

No actual tool execution, browser/WebGL/canvas runtime, public artifact, signed URL, beta, or production unlock occurred.

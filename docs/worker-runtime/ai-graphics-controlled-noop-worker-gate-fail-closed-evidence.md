# Fail-Closed Evidence

The executor validates fail-closed metadata assertions for missing plan snapshots, missing scoped manifests, runtime requests, public artifact requests, and signed URL requests. Requests that cross those boundaries remain blocked before execution.

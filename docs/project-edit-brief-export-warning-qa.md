# Project Edit Brief Export Warning QA

Marker QA can produce export-setting warnings when marker guidance conflicts with session-level Export Settings. Examples include caption markers with caption safe area off, vertical or caption-heavy marker language while export settings are wide, and custom export settings without usable resolution metadata.

Export warnings are mock/local readiness metadata. They do not start render, export, preview, media probing, file reads, workers, providers, credits, or Supabase commands.

There is no Qwen, no DeepSeek, no providers, no workers, no render, no credits, no Supabase command, no media processing, no file-byte read, no external URL fetch, and no planner application.

owner review remains pending for production export validation and render readiness.

Marker QA boundary summary: mock/local, no Qwen, no DeepSeek, no providers, no workers, no render, no credits, no Supabase command, no planner application, owner review pending.

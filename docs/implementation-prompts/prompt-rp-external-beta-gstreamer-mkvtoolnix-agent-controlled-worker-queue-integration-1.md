# RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-AGENT-CONTROLLED-WORKER-QUEUE-INTEGRATION-1

Use only after `RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-AGENT-EXECUTION-BRIDGE-CONTROLLED-DISPATCH-1` is merged with decision `completed_gstreamer_mkvtoolnix_agent_execution_bridge_controlled_dispatch_boundary`.

The next packet may plan or implement a confirmation-gated worker queue integration boundary for the accepted GStreamer/MKVToolNix external-agent dispatch metadata. It must preserve the approved snapshot, approval record, no-spend or reserved-credit policy, job, lease, idempotency, command-template allowlist, private input manifest, output manifest schema, QA report schema, cleanup policy, retention policy, failure policy, audit parent, and runtime evidence references.

The queue integration packet must still fail closed unless it has an explicit confirmation gate. It must not accept raw commands, raw chat, arbitrary file paths, public URL source-of-truth, signed URL source-of-truth, arbitrary private media, FFmpeg/FFprobe expansion, Docker push/deploy, Remotion rendering, Supabase mutation, SQL, public artifacts, final render/export, broad external beta unlock, paid production unlock, or production unlock.

The queue integration packet may only mark a metadata job as ready for a later worker-dispatch proof if it records the exact queue contract, idempotency key, job payload, retry/failure policy, audit event, and non-public artifact policy. Any actual worker dispatch or tool execution remains a separate guarded milestone.

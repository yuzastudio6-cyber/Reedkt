# Runtime Target Review

- Future target: `docker/prod/cpu-worker/Dockerfile` with `docker/prod/cpu-worker/requirements.cpu.txt`.
- `docker/prod/pro-color-image-runtime` remains Track A/pro color-image context only.
- No Docker build or runtime target mutation is approved now.

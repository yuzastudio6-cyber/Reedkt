# RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-WORKER-DISPATCH-RUNTIME-EXTERNAL-AGENT-EXECUTION-QA-1

Use only after `RP-EXTERNAL-BETA-GSTREAMER-MKVTOOLNIX-WORKER-DISPATCH-RUNTIME-EXTERNAL-AGENT-EXECUTION-PACKET-1` is merged with decision `completed_gstreamer_mkvtoolnix_worker_dispatch_runtime_external_agent_execution_packet`.

The QA packet must verify the execution packet source chain, local evidence manifest, safety boundary, unchanged `package-lock.json`, and generated-artifact exclusion. It must keep route execution, external-agent runtime invocation, worker process start, worker lease mutation, persistent queue writes, tool execution, media processing, Supabase, SQL, signed/public artifacts, production unlock, and final render/export blocked unless a later guarded runtime execution packet explicitly authorizes them.

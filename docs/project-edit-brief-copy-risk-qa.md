# Project Edit Brief Copy-Risk QA

RP-EDITBRIEF-10 blocks or warns on marker language that asks to copy a reference exactly, recreate a shot shot-for-shot, match a third-party video exactly, or use a reference URL as footage. The safe resolution is always adapted, not copied: describe transferable editing language instead of exact timing, creator identity, layout, music, SFX, footage, or shot replication.

Reference URLs stay metadata-only and are never fetched. Marker QA can show a copy-risk finding and conflict-style review card, but it never downloads, inspects, or uses reference media.

There is no Qwen, no DeepSeek, no providers, no embeddings, no vector DB, no workers, no render, no credits, no Supabase command, no file-byte read, no URL fetch, no media processing, and no planner application.

owner review remains pending before any do-not-copy policy is enforced in production.

Marker QA boundary summary: mock/local, no Qwen, no DeepSeek, no providers, no workers, no render, no credits, no Supabase command, no planner application, owner review pending.

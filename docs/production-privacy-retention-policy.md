# Production Privacy Retention Policy

Retention classes include source media, proxy media, analysis artifacts, transcripts, masks, generated assets, previews, final exports, worker temp, and QA artifacts.

All classes default to private storage. User deletion requests must cascade to derived artifacts while preserving sanitized audit summaries where policy permits.

Worker temp data must be cleaned after completion, retry exhaustion, lease expiry, and startup sweeps.

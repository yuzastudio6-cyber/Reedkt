# Production Rate Limit Policy

M17 defines placeholder rate limits for:

- per-workspace job creation;
- per-user upload requests;
- per-user render requests;
- per-project concurrent jobs.

These placeholders are static policy definitions. Human operators must approve final values before external beta or production.

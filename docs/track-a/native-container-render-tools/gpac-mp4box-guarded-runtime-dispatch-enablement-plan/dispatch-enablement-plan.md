# Dispatch Enablement Plan

Future runtime dispatch requires `REEDITPRO_CONFIRM_TRACKA_GPAC_MP4BOX_GUARDED_RUNTIME_DISPATCH=true` in a later, explicitly scoped packet.

The future packet must include:

- an immutable approved snapshot reference;
- confirmed frame and timing state;
- a credit reservation reference;
- a private artifact manifest with SHA-256 checksums;
- cleanup and residue readback requirements;
- service-role backend-only route boundaries;
- idempotency keys, worker leases, and worker event records;
- a bounded MP4Box command allowlist;
- explicit rejection of raw chat execution, arbitrary user media, broad service-role handlers, signed/public artifacts, and product unlock drift.

This planning packet does not authorize runtime dispatch.

Route execution: `false`.

Worker dispatch: `false`.

Worker execution: `false`.

GPAC/MP4Box execution: `false`.

Media processing: `false`.

Storage transfer: `false`.

# Phase 44F Desktop Benchmark Fixtures

Phase 44F uses generated/mock benchmark fixtures only:

- `fixture-benchmark-high-desktop`
- `fixture-benchmark-mid-desktop`
- `fixture-benchmark-low-desktop`
- `fixture-benchmark-parallel-unavailable`
- `fixture-benchmark-policy-blocked`

The fixtures validate local light-task hints, server-worker preference, unavailable media runtimes, parallel-worker limitations, local temp storage hints, cost-estimator input hints, and fail-closed behavior when local benchmark confirmation is missing.

# Cleanup Evidence

Local evidence is written only under `.local-artifacts/worker-runtime/ai-graphics-controlled-noop-worker-gate/ai-graphics-controlled-noop-worker-gate-local-static/`.

The committed packet contains sanitized Markdown evidence only. `.local-artifacts` is ignored and must not be staged.

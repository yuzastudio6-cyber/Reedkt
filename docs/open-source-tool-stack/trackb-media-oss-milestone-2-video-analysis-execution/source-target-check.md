# Source Target Check

Target Dockerfile: `docker/prod/cpu-worker/Dockerfile`.

Target requirements: `docker/prod/cpu-worker/requirements.cpu.txt`.

Required packages declared: `true`.

Dockerfile requirements install detected: `true`.

# Ranking QA

The limited internal beta dry-run QA accepts the existing Track B ranking order. The order remains least-risky matching tool first, with metadata/probe tools before heavier analysis, OpenColorIO before OpenImageIO in the color/image path, deterministic OCR before ML OCR, and FFmpeg last as the explicit transform route.

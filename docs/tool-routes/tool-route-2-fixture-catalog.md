# TOOL-ROUTE-2 Fixture Catalog

- `provider_model_planning_fixture`: route `provider_model_planning`; owner `PROVIDER_GATEWAY_MODELS`; synthetic input only `true`; output manifest `synthetic_provider_output_fixture_manifest`.
- `worker_runtime_job_planning_fixture`: route `worker_runtime_job_planning`; owner `WORKER_RUNTIME_JOBS`; synthetic input only `true`; output manifest `synthetic_worker_job_fixture_manifest`.
- `web_search_capture_fixture`: route `web_search_capture`; owner `WEB_SEARCH_CAPTURE`; synthetic input only `true`; output manifest `synthetic_web_search_capture_fixture_manifest`.
- `map_geospatial_fixture`: route `map_geospatial`; owner `MAP_GEOSPATIAL`; synthetic input only `true`; output manifest `synthetic_map_geospatial_fixture_manifest`.
- `ai_tools_creative_graphics_fixture`: route `ai_tools_creative_graphics`; owner `AI_TOOLS_CREATIVE_GRAPHICS`; synthetic input only `true`; output manifest `synthetic_ai_tools_creative_graphics_fixture_manifest`.
- `track_a_render_export_fixture`: route `track_a_render_export`; owner `TRACK_A_RENDER_EXPORT`; synthetic input only `true`; output manifest `synthetic_track_a_render_export_fixture_manifest`.
- `track_b_media_processing_fixture`: route `track_b_media_processing`; owner `TRACK_B_MEDIA_PROCESSING`; synthetic input only `true`; output manifest `synthetic_track_b_media_processing_fixture_manifest`.
- `sound_music_audio_fixture`: route `sound_music_audio`; owner `SOUND_MUSIC_AUDIO`; synthetic input only `true`; output manifest `synthetic_sound_music_audio_fixture_manifest`.
- `supabase_metadata_storage_fixture`: route `supabase_metadata_storage`; owner `SUPABASE_RLS_STORAGE_DATABASE`; synthetic input only `true`; output manifest `synthetic_supabase_metadata_storage_fixture_manifest`.
- `observability_audit_cost_fixture`: route `observability_audit_cost`; owner `OBSERVABILITY_AUDIT_COST`; synthetic input only `true`; output manifest `synthetic_observability_audit_cost_fixture_manifest`.
- `compliance_security_fixture`: route `compliance_security`; owner `COMPLIANCE_SECURITY`; synthetic input only `true`; output manifest `synthetic_compliance_security_fixture_manifest`.
- `frontend_product_ux_fixture`: route `frontend_product_ux`; owner `FRONTEND_PRODUCT_UX`; synthetic input only `true`; output manifest `synthetic_frontend_product_ux_fixture_manifest`.
- `billing_stripe_credits_fixture`: route `billing_stripe_credits`; owner `BILLING_STRIPE_CREDITS`; synthetic input only `true`; output manifest `synthetic_billing_stripe_credits_fixture_manifest`.
- `public_artifact_signed_url_delivery_blocked_fixture`: route `public_artifact_signed_url_delivery_blocked`; owner `COMPLIANCE_SECURITY`; synthetic input only `true`; output manifest `synthetic_public_artifact_signed_url_blocked_fixture_manifest`.

No Supabase mutation, SQL execution, Google Cloud API call, Secret Manager API call, provider call, model call, tool execution, worker execution, route execution, browser capture, Docker/Cloud Run execution, storage transfer, signed URL creation, public artifact creation, credit mutation, Stripe checkout/webhook/payment processing, deployment, internal beta unlock, external beta unlock, production unlock, dependency mutation, raw prompt execution, final render/export, or broad service-role handler was enabled.

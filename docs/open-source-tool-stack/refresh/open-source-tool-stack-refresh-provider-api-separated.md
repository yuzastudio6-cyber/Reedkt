# Open-Source Tool Stack Refresh Provider API Separation

The canonical provider/API separated count remains `3`.

Provider/API items remain separated from local OSS tool candidates. This refresh did not call providers, execute model runtime, fetch provider output, or add provider SDK readiness.

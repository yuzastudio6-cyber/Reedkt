# AI Graphics Job Payload Private Artifact Gate Status Owner Approval

Decision: `worker_ai_graphics_metadata_job_payload_dry_run_gate_status_owner_approved_with_warnings`

Owner approval accepts private artifact/checksum mapping with warnings. All 13 tools retain placeholder `<PRIVATE_ARTIFACT_MANIFEST_REF>` and `<CHECKSUM_REF>` requirements; public artifacts and signed URLs remain rejected as source truth.

Status: `accepted_with_warnings`.

# Prompt Phase 44F Desktop Benchmark Runner

Implement Phase 44F only after Phase 44E is reviewed and explicitly approved.

Goal: define and run a bounded desktop benchmark runner for future Track B route/cost planning. This phase must not execute media/audio/OCR/VLM/model runtimes, providers, routes, local worker sidecars, Docker, Cloud Build, Cloud Run, GCP/IAM mutation, beta, production, or Track A unless separately approved by the prompt.

Phase 44F must preserve privacy by avoiding persistent identifiers, usernames, hostnames, MAC addresses, path scans, environment dumps, process lists, installed app scans, exact CPU model strings, exact GPU IDs, and public artifacts.

# Canonical Agent Selection Capability Map

Decision: `ai_graphics_canonical_agent_selection_review_passed_with_warnings`.

- `chart_overlay`: candidates `vega_lite`, `d3`, `echarts`, `vega`; execution false.
- `data_visualization`: candidates `vega_lite`, `vega`, `d3`, `echarts`; execution false.
- `svg_graphics`: candidates `svgdotjs_svg_js`, `satori`, `d3`; execution false.
- `diagram_graphics`: candidates `viz_js`, `svgdotjs_svg_js`; execution false.
- `animation_overlay`: candidates `lottie_web`, `animejs`; execution false.
- `canvas_scene`: candidates `pixi_js`, `konva`; execution false.
- `webgl_3d_scene`: candidates `three_js`, `babylonjs`; execution false.
- `background_removal`: candidates `sam2`, `birefnet`, `rembg`, `transparent_background`; execution false.
- `subject_segmentation`: candidates `sam2`, `birefnet`; execution false.
- `upscaling`: candidates `real_esrgan`; execution false.
- `tensor_image_ops`: candidates `kornia`; execution false.
- `model_runtime_foundation`: candidates `torch_torchvision`, `transformers`; execution false.

# Validation Command Plan

Validation is no-install and no-runtime. It checks this delivery/share packet, all predecessor readiness diagnostics, Track B source-truth diagnostics, and Git whitespace checks.

No `npm ci`, Docker, Supabase/GCS write, SQL, migration, worker dispatch, provider call, media processing, render/export, public artifact, signed URL, external beta, or production action is authorized.

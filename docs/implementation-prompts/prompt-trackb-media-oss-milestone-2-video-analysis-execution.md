# TRACKB_MEDIA_OSS_MILESTONE_2_VIDEO_ANALYSIS_EXECUTION

Execute the approved Track B Milestone 2 CPU-first install/proof lane for OpenCV, PyAV, and PySceneDetect only, using the existing CPU worker target `docker/prod/cpu-worker/Dockerfile` and `docker/prod/cpu-worker/requirements.cpu.txt`.

Approved future packages are `opencv-python-headless`, `av`, and `scenedetect`. Use tiny synthetic fixtures only. Do not use real user media, public artifacts, signed URLs, beta, production, or FFmpeg/FFprobe expansion unless a later prompt explicitly approves that scope.

Expected source decision before execution: `trackb_media_oss_milestone2_video_analysis_approval_passed_ready_for_execution`.

Supabase classification remains: no write / environment none / SQL none / migration no.

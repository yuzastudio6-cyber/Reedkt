# AI Graphics CPU Static Spec Validation Approval: Viz.js

Decision: `ai_graphics_cpu_static_spec_validation_approval_passed_with_warnings`

Tool: `viz_js`

Canonical proof level: `canonical_merged_package_import_static_fixture_proof`

Future approval: `vizJsFutureStaticValidationApproved=true`

Fixture plan: small DOT graph fixture to DOT/SVG metadata contract later, no public artifact.

Output contract: DOT/SVG metadata only; no committed SVG output, public artifact, signed URL, browser render, or export.

Blocked now: `staticValidationExecutionApprovedNow=false`, `staticFixtureExecutionApprovedNow=false`, `toolExecutionPerformed=false`, `browserRuntimeApprovedNow=false`, `toolRouteExecutionApprovedNow=false`, `workerExecutionApprovedNow=false`, `runtimeReadyNow=false`, `internalBetaReadyNow=false`, `externalBetaReadyNow=false`, `productionReadyNow=false`.

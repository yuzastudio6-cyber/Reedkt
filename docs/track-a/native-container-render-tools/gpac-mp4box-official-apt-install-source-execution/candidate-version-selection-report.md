# Candidate Version Selection Report

Selected candidate: `26.02-rev0-g118e60a90-HEAD`.

Architecture: `arm64`.

The build evidence recorded `apt-cache policy gpac` from `https://dist.gpac.io/gpac/linux/debian bookworm/main arm64 Packages`. The candidate was non-empty, not `(none)`, and the policy output did not contain `nightly`.

Visible versions in the build evidence were `26.02-rev0-g118e60a90-HEAD` and `2.4-rev0-g5d70253ac-HEAD`; the install used the exact candidate form `gpac=26.02-rev0-g118e60a90-HEAD`.

# Boundary Review

Accepted scope:

- one generated synthetic subtitle fixture;
- one MP4Box subtitle import command;
- one MP4Box info command;
- network-disabled container execution;
- transient `/tmp` artifacts only.

Blocked scope remains: user/private/real media, arbitrary media probing, FFmpeg/FFprobe, GStreamer, MKVToolNix, Bento4, VapourSynth, render/export, workers/routes/providers, Supabase/GCS, public artifacts, signed URLs, beta, production, and product runtime approval.

Product-ready local OSS tools remain `0`; Track B FFmpeg/FFprobe ownership remains preserved; #577 remains excluded.

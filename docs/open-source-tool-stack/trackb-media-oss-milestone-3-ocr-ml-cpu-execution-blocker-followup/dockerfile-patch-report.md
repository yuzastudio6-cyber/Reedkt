# Dockerfile Patch Report

Target Dockerfile: `docker/prod/ocr-runtime/Dockerfile`.

Approved package: `libgomp1`.

Patch applied: `true`.

Only approved package added: `true`.

Requirements, package-lock, .dockerignore, and unrelated Dockerfiles were not intentionally mutated.

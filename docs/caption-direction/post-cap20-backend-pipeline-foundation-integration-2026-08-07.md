# Post-CAP-20 backend-pipeline foundation integration — 2026-08-07

Milestone: `Caption specialist -> canonical backend workflow foundation`

Status: `source_integrated_and_regressed_terminal_evidence_still_open`

## Outcome

The published Caption specialist checkpoint at
`f27da22875a4f07d3dd732a0e0e20e0998c245d1` is now merged with the published
canonical backend workflow foundation at
`8fdc3cd14f7ba2ee7ab5bcc6f30f5273a65f1277` on the reviewable integration
branch `codex/backend-caption-specialist-pipeline-integration-v1`.

The merge commit is `c75dda7aec5ea48d137e855ab7a3ac06d1b30045` with tree
`5baaa08f479be0be3b468ed004a73f7fe3d29a7b`. A pre-merge `git merge-tree`
check and the actual `ort` merge both completed without a content conflict.

This is the requested source-level handoff into the backend workflow pipeline.
It is not a production deployment and does not promote any missing private
evidence gate.

## Files changed

The merge imports the backend foundation's additive SAM 3.1 image,
supply-chain, security-closure, operator, service, and regression sources. The
only integration-authored source repair is:

- `server/smoke/canonical-sam3_1-qualification-capsule-builder-smoke.ts`.

This report and the Caption Direction document index are also additive.

## Contracts added or changed

No Caption public DTO, Caption/Living Frame V1/V2 compatibility contract,
Caption job contract, manifest identity, or qualification projection was
changed. The backend parent contributes only its own published contracts and
runtime candidates.

## Existing owners reused

- canonical backend approved-snapshot, work-graph, asset, estimate/cost,
  runtime, private-review, and final-QA owners;
- Visual Intelligence provider-neutral owner;
- Track All/SAM 3.1 mask and tracking owner;
- Caption-owned planning, scene-graph, render-spec, QA, repair, and support
  boundaries;
- StoryTiming, Remotion, libass, FFmpeg, SoundSync, B-roll, and Living Frame
  boundaries already present in the Caption checkpoint.

## Duplicate owners avoided

No central Orchestra, peer dispatcher, Caption SAM runner, Caption Visual
Intelligence provider, Caption billing owner, Caption final-QA owner, or second
approved-snapshot/work-graph/asset owner was added. The original source tree
under `/Volumes/backup/REeditpro` remained read-only.

## Integration defect and repair

The first full backend Visual Intelligence/GPU release run rejected the merged
tree because the qualification-capsule smoke still expected the historical
PEP-668 package-removal command while the current reviewed candidate uses the
required one-time `--break-system-packages` uninstall flag. The smoke also
forbade that same flag globally.

The repair updates the regression to require exactly one occurrence of the
reviewed uninstall flag. It does not change the Docker candidate, weaken the
offline runtime, permit runtime dependency resolution, or grant build/runtime
authority.

## Tests run

- `npm run smoke:captions-specialist-source-integration-aggregate`;
- `npm run typecheck:server`;
- `npm run smoke:visual-intelligence-quality-gpu-release`;
- focused `canonical-sam3_1-qualification-capsule-builder` smoke;
- `npm run qa:visual-intelligence-quality-gpu-release`.

## Tests passed

- Caption aggregate: 49/49 source suites, 41/41 job implementations, 41/41
  source paths, and 0 pending composition mounts;
- server typecheck: passed;
- shared release summary: passed with live Gemini, live GPU, checkpoint
  download, credits, and production all false;
- corrected capsule-builder smoke: passed;
- complete shared Visual Intelligence/GPU release gate: 119/119 source smokes.

## Tests failed

One initial shared release invocation failed on the stale capsule-builder
assertion described above. The defect was repaired and the entire 119-smoke
gate then passed. No failure is hidden or reclassified.

## Media inspected

No new media was generated by this source integration. The previously sealed
real-source `2160x3840@30` render and all-127-frame direct inspection remain
the current Caption visual evidence; this merge does not relabel them as
shared Visual Intelligence or independent final QA.

## Visible defects

None were assessed because this milestone did not generate media.

## Repairs made

- reconciled the SAM qualification-capsule regression with the exact current
  reviewed PEP-668 removal command;
- reran the complete shared release suite after the repair.

## Known limitations and scoped blockers

The Caption implementation is connected to the latest backend source
foundation, but terminal private qualification remains open. The exact
canonical campaign still requires real accepted owner evidence for the
corrected transcript, qualified Visual Intelligence, Track All/SAM 3.1,
complete-time SoundSync listening review, all representative Caption runs,
shared postrender visual review, and independent final QA/private review.

This agent runtime was explicitly tested for audio input and reported that
audio input is unsupported. Therefore it did not fabricate the pending
independent transcript or SoundSync listening reviews.

## Safe work completed

The Caption specialist is now on a reviewable backend-pipeline integration
branch with both parents preserved, no copied divergent checkout files, and a
green shared source release gate.

## Next milestone

Continue the private campaign through existing owners as real reviewer/model
evidence becomes available. Preserve `0/41` terminal jobs and `0/9` terminal
gates until the first eight gates pass inside one exact persisted canonical
campaign and the final per-job projection is create-only persisted and reread.

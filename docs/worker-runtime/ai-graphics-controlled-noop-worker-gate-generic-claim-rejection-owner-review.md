# Generic Claim Rejection Owner Review

Owner review result: `accepted_with_warnings`

`genericDryRunPassedClaimed`, `dryRunPassedClaimed`, and `generatedLocalFixturePassedClaimed` remain `false`. Their matching accepted booleans remain `false`.

This owner review accepts only scoped Worker AI graphics metadata claims and does not accept a generic dry-run or generated-local fixture pass claim.

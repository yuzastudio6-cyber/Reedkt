# Component And Pinning Policy

Selected component: `main`

Blocked component: `nightly`

Future package name candidate: `gpac`

Observed package versions in the `main` amd64 package index:

- `2.4-rev0-g5d70253ac-HEAD`
- `26.02-rev0-g118e60a90-HEAD`

Because multiple versions are visible in the package index, the next official APT approval lane must specify a version pin or apt preference before any execution lane may install `gpac`.

Execution approved in this phase: `false`.

# TRACKA-THREE-TOOL-EXTERNAL-AGENT-ROUTE-WORKER-DISPATCH-DRY-RUN-1

Use only after `TRACKA-THREE-TOOL-EXTERNAL-AGENT-EXECUTION-BRIDGE-DRY-RUN-1` is merged and validated.

Required confirmation gate:

`REEDITPRO_CONFIRM_TRACKA_THREE_TOOL_EXTERNAL_AGENT_ROUTE_WORKER_DISPATCH_DRY_RUN=true`

The route-worker dispatch dry run may validate guarded route handler metadata, idempotency keys, job/lease references, approved snapshot references, child bridge handoff envelopes, artifact manifest schemas, QA schema references, cleanup policy references, and fail-closed status mapping.

It must not execute GStreamer, MKVToolNix, GPAC/MP4Box, Docker, FFmpeg/FFprobe, Remotion, private/user media, Supabase, SQL, workers, providers, signed/public artifacts, final render/export, broad external beta expansion, paid production, or production.

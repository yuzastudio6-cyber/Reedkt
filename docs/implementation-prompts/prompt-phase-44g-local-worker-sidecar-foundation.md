# Prompt Phase 44G Local Worker Sidecar Foundation

Implement Phase 44G only after Phase 44F and Phase 44H are reviewed and explicitly approved.

Goal: define local worker sidecar planning and lifecycle boundaries for future Track B local execution. This phase must not process broad media, accept arbitrary paths, execute providers, run VLM/OCR/Demucs/DeepFilterNet/Signalsmith tools, mutate cloud/IAM, unlock beta/production, or touch Track A unless separately approved.

Phase 44G must require approved plan snapshots, artifact scopes, private prefixes, operator confirmations, strict shutdown, logging redaction, and rollback/blocker policy before any local sidecar execution path can be considered. Phase 44G itself is protocol/policy/fixture metadata only.

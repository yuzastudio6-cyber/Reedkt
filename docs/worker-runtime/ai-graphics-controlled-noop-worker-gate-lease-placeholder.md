# AI Graphics Controlled No-Op Worker Gate Lease Placeholder Policy

Future controlled no-op validation may inspect placeholder lease fields only.
It must not create, refresh, mutate, or release a real lease.

Lease references remain static metadata checks tied to the approved plan
snapshot, scoped manifest, private artifact placeholders, and fail-closed
assertions.

`workerLeaseMutationApprovedNow` remains `false`.

# Dependency Baseline Repair Decision

Decision: `dependency_baseline_repair_passed_ready_for_batch_1_approval_rerun`.

The central dependency baseline now validates with `npm ci --ignore-scripts --no-audit --no-fund` after the narrow `@emnapi/*` lock metadata repair.

Ready for Batch 1 approval rerun: `true`

Ready for install/proof execution: `false`

Next prompt: `OPEN_SOURCE_TOOL_STACK_INSTALL_PROOF_APPROVAL_BATCH_1_RERUN_AFTER_DEPENDENCY_REPAIR`.

All real tool, route, worker, provider, media/audio/render/image/browser/map, Supabase/SQL/GCS, public artifact, signed URL, raw prompt, beta, and production scopes remain blocked.

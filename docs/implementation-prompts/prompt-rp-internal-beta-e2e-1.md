# RP-INTERNAL-BETA-E2E-1

Use this prompt only after the data, backend job queue, internal credit ledger, private artifact storage, and render-worker skeleton gates pass.

Prove one narrow internal beta lane: private test upload, structured intent, source sequence map, edit plan, credit estimate, user approval, approved plan snapshot, internal credit reservation, worker enqueue, private preview/export artifact, QA report, manifest/checksum, cleanup record, and private readback.

Do not unlock external beta, paid production, broad media, public artifacts, final delivery/export, frontend provider calls, raw-chat worker execution, Supabase service-role writes from the frontend, unapproved tool execution, or production routes.

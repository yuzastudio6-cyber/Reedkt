# Production Beta Readiness Runbook

Internal dry-run testing may use static reports and generated mock artifacts. Local-dev fixture testing may use generated fixtures only when explicitly enabled and safe.

Real user media beta requires human approval for deployment, storage, security, cost controls, retention/deletion, model weights, licenses, monitoring, and support.

External beta and paid production remain blocked by default in M17 only for the unsafe launch/runtime surfaces they protect. That default does not block bounded source review, local proof, no-write previews, diagnostics, QA, owner remediation, monitoring, rollback planning, or other blocker-reduction work that keeps beta/production gates closed while collecting the missing evidence.

## Tools Evidence Handoff

After the local accepted evidence snapshot passes, operators must first prove the staging API deploy inputs, then deploy or verify the staging API service, then run `npm run beta:readiness:api-deployment-preflight`. The earlier guarded manual discovery and owner-remediation workflows captured owner-side IAM gaps that existed before the current staging services were created. Current source-truth evidence after `769fc2d922b37a9eebb8b0ca29fa2447a6f8f127` shows the normal staging API now runs as `reeditpro-api-staging` revision `reeditpro-api-staging-00012-ncd` in `us-east1`, and the deployed tool-readiness service remains `reeditpro-tool-readiness-staging` revision `reeditpro-tool-readiness-staging-00002-qdp`. Both services are authenticated-only and staging-only.

The current-source API deploy evidence is `docs/beta-readiness/api-staging-deploy-current-source/2026-06-29-769f-api-staging-deploy.md`. It records GitHub Actions run [28343028750](https://github.com/yuzastudio6-cyber/Reedkt/actions/runs/28343028750), image `us-central1-docker.pkg.dev/reeditpro/reeditpro-staging-workers/reeditpro-api:api-staging-769fc2d922b3-20260629T0129Z-retry1`, digest `sha256:9a5bcb232c17b5a0eba14158063d5d2828d56be06d56fb7bdf66caf9d1a042d5`, revision `reeditpro-api-staging-00012-ncd`, and public unauthenticated `/health` readback returning HTTP `403` for both staging URLs. The previous `17a9a2d2b015ab325cf13ce5135d083af070ab00` deploy packet remains historical context. The current-source API deployment preflight packet `docs/beta-readiness/api-deployment-preflight/2026-06-29-769f-api-deployment-preflight-passed.md` passed with the exact deployed source SHA, image, service account, API URL, and secret-binding confirmations. Local authenticated readback and the platform technical probe were not rerun at `769fc2d922b37a9eebb8b0ca29fa2447a6f8f127`; those remain next-step evidence after owner approvals and authenticated access are available.

The current deployed evidence input manifest classification is `docs/beta-readiness/deployed-evidence-input-manifest/2026-06-29-769f-deployed-evidence-input-manifest.md`, with the earlier owner-input gap packet at `docs/beta-readiness/deployed-evidence-input-manifest/2026-06-29-17a9-technical-inputs-owner-approval-gap.md` preserved as the detailed 29-input owner checklist source. It proves the current deploy readback is source-fresh for `769fc2d922b37a9eebb8b0ca29fa2447a6f8f127` while the manifest remains fail-closed on 29 missing owner approval or attestation inputs. `docs/beta-readiness/source-freshness-preflight/2026-06-29-769f-source-freshness-passed.md` records the 769f deploy evidence; the live source freshness preflight now passes from `ba5bc11778db7c3e2efd6b56941949f64b2c9684` as metadata-only source drift, while runtime-affecting drift remains blocked. Collect owner inputs through `docs/beta-readiness/owner-approval-packet-current-gates/2026-06-28-owner-approval-packet-current-gates.md`, use `npm run beta:readiness:owner-approval-env-template` to generate an exact local fill-in template, then run the freshness preflight and `npm run beta:readiness:owner-approval-intake-preflight` before any evidence collector is run.

This owner-approval gap is a scoped blocker, not an intentional blanket stop. It blocks only external beta launch, real-user-media beta, paid production launch, provider calls, worker dispatch, Supabase/GCS writes, public artifact delivery, and signed URL delivery. Safe blocker-reduction work remains allowed through owner approval collection, deployed-evidence preflights, diagnostics/QA packets, monitoring, support, and rollback planning.

The current platform technical probe packet remains `docs/beta-readiness/platform-technical-probe-current-state/2026-06-28-a735-platform-technical-probe.md`. It records historical stored workspace evidence with `productReadyLocalOssCount: 14`, while the current Track B source posture is `16 owned / 16 bounded accepted-proven / 0 blocked-not-installed-proven / 16 product-ready` for the ranked tool-call lane and active beta deployed readback remains `0` until product-ready evidence is recorded/read back from staging. The normal API report-only platform probe passed deployed migration, service-role write, RLS readback, idempotent replay, wallet settlement, monitoring, and staging billing-QA checks. For this platform evidence packet, recording remains blocked by billing-owner Stripe-boundary approval plus deployment, security, storage/privacy, legal, monitoring, and support owner approvals; broader launch, model/license, real-user-media beta, and paid-production gates remain separate blocked lanes.

Use `npm run beta:readiness:owner-approval-packet` or `docs/beta-readiness/owner-approval-packet-current-gates/2026-06-28-owner-approval-packet-current-gates.md` for the current non-secret owner approval request packet. Then use `npm run beta:readiness:owner-approval-collection-handoff` or `docs/beta-readiness/owner-approval-collection-handoff/2026-06-29-769f-owner-approval-collection-handoff.md` for the exact 29-input collection checklist. These packets replace the older IAM-remediation handoff as the next action after the staging API and tool-readiness services exist. They do not grant approval; they name the required owner roles, evidence notes, forbidden credential-bearing evidence, and the preflight/collector commands that must pass after owners provide approvals.

The guarded manual workflow `.github/workflows/beta-readiness-api-staging-deploy.yml` is the source-controlled staging deploy path after those inputs are proven: it runs only through `workflow_dispatch` from default branch `codex/reeditpro-web-ui-shell`, requires `DEPLOY_STAGING_BETA_READINESS_API`, fetches `source_ref=codex/sound-music-audio-1abc-checkpoint`, requires an exact source SHA, locks the service to `reeditpro-api-staging`, rejects `latest` image tags, uses Workload Identity, binds Secret Manager names only, and keeps `--no-allow-unauthenticated`. The preflight verifies the API URL, deployed source SHA, API image, service account, staging-only environment, and Secret Manager binding confirmations without running `gcloud` or touching the deployed backend.

After the API deployment preflight, source freshness preflight, and owner approval intake preflight pass, operators must run `npm run beta:readiness:deployed-evidence-input-manifest` before any deployed evidence collector. The latest deployed evidence now uses separate endpoint responsibilities: tool evidence should target the tool-readiness service because it contains the core command/import dependencies, while platform evidence, launch approval evidence, and final status readback may target the normal API service. The manifest should bind the deployed source SHA, accepted 16-tool bounded-readiness readback, the libass image/mode, staging-only platform packet, launch owner approvals, and separate idempotency keys into one no-network readiness check.

Only after that manifest, platform evidence preflight, launch approval preflight, and deployed operator inputs all pass should `npm run beta:readiness:external-beta-evidence-collector` be used against staging. That collector records evidence and then requires final operator-status readback; it still does not approve real-user-media beta or paid production.

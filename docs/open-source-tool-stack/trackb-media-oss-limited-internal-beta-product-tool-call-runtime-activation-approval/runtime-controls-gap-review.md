# Runtime Controls Gap Review

The dry-run lane proves metadata shape, not live control behavior. A separate control plan is required before enabling any direct product tool call, route runtime, worker dispatch, write path, monitoring emission, rollback action, beta exposure, or product-ready status.

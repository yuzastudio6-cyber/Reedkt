# Living Frame AuraFace Continuity Measurement

Status: deterministic vector math and the process-bound CPU-runtime lease
bridge are implemented; released AuraFace inference, identity decision, QA
approval, persistence, costing, and production gates remain closed.

This contract implements the comparison step for the sixth Living Frame
controlled-illustration capability. It does not itself run AuraFace. A
process-bound, single-use server reader supplies two 512-component `Float32`
embeddings out of band. They may come from a controlled fixture or from the
unreleased private CPU-runtime output lease; the two evidence classes cannot
be mixed or relabeled. The contract validates exact lineage, face counts,
type, length, finite values, non-zero norms, and non-shared buffers before
calculating cosine similarity.

The score is represented as an integer in millionths from `-1,000,000` to
`1,000,000`. The result contains no embedding, image, path, URL, credential,
identity reference, provider identifier, or customer-commercial amount.

## Deliberate non-decision

Cosine similarity is evidence, not identity truth. The contract:

- accepts no caller threshold;
- applies no universal threshold;
- cannot approve identity, likeness, or historical accuracy;
- cannot promote an illustrative interpretation into verified evidence;
- requires project calibration, fairness review, and user review;
- treats no-face or multiple-face inputs as blocking rather than scoring them;
- marks the score as private, sensitive, non-browser-shareable, and
  non-persistable by this contract; and
- never serializes or persists the source embeddings.

Neither the controlled fixture class nor the private internal runtime
observation can be relabeled live, verified, released, or production-ready.
A released server-owned AuraFace operation still requires separate evidence
through the canonical model-artifact, dispatch, attempt, cost, privacy,
consent, and QA authorities.

## Cost boundary

When AuraFace eventually runs, it is one separate bounded CPU measurement
attempt, outside the shared ComfyUI L4 generation attempt. Exact reused
measurement evidence adds no new attempt. Failed and unknown attempts remain
attributable through the existing internal tool-cost authority.

This vector-math contract contains no attempt amount, customer price, credits,
service fee, reservation, wallet, or ledger state. Customer credits must still
aggregate and round once through the canonical estimate system, with the
service fee applied once downstream.

## Still closed

Artifact ingest/mount, ONNX Runtime and preprocessing qualification, private
AuraFace inference, consent and likeness policy, minor/documentary safety,
retention/deletion/access policy, project threshold calibration, fairness
evaluation, canonical attempt/completion/cost evidence, selected-scene
lineage, QA approval, private review, rendering, and production remain closed.

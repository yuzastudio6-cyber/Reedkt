# AI Graphics CPU/Static Runtime Boundary

Decision: `ai_graphics_draft_package_proof_runtime_boundary_review_passed_with_warnings`

Future lane: `cpu_static_spec_validation_later`.

Tools:

- `d3`
- `vega_lite`
- `vega`
- `satori`
- `svgdotjs_svg_js`
- `viz_js`

Current status: canonical package/import/static-fixture proof only. CPU/static runtime is not approved now. The future lane must separately approve static spec validation inputs, outputs, artifact scope, and no public artifact behavior before any execution.

Current booleans: `cpuStaticRuntimeApprovedNow=false`, `agentExecutionAllowedNow=false`, `toolExecutionPerformed=false`, `publicArtifactCreated=false`.

# Production Color Look Transform Policy

Color correction starts clean and natural before creative looks.

Default intensity is `0.35`. LUT strength defaults to `0.25` when a LUT is used, warns above `0.45`, and blocks outside `0` to `0.6`. Strong looks can be planned only as capped metadata and must pass color QA before any later preview/export milestone.

# Tool Beta Execution Readiness Gate

This gate answers whether the registered ReEditPro production tools are ready for external beta or paid production execution.

Current status in this branch:

- Production registry tools: 49
- Tool cost owner coverage records: 49
- Production readiness specs: 49
- Product-ready local OSS tools: 0
- Tool-specific blockers: 184
- Platform blockers: 1 billing deployment/QA gate
- External beta tool execution: blocked
- Paid production tool execution: blocked

## What The Gate Checks

- Every `ProductionToolId` has a metering owner case.
- Every `ProductionToolId` has a production readiness spec.
- Every tool records worker/image readiness boundaries.
- Every tool remains behind approved plan, credit estimate, credit reservation, and idempotent event gates.
- Real command/import/container execution remains required before external beta.
- Model/checkpoint-backed tools remain blocked until model source, license, checksum, and staging approvals pass.
- Durable tool cost event persistence now has a Supabase-backed source skeleton and local migration artifact.
- Billing deployment remains a platform blocker until the migration, RLS/service-role path, wallet settlement, Stripe boundary, monitoring, and billing QA are verified.
- Product-ready local OSS remains `0` until a later QA gate accepts real runtime evidence.

## Evidence Acceptance

The tool execution gate is no longer a permanent hardcoded `false` for product readiness. A future QA packet can provide accepted per-tool evidence with:

- a known `toolId`;
- a non-empty source ID;
- passed or warning readiness status;
- real execution verification;
- production-readiness acceptance;
- product-ready local OSS acceptance;
- model/checkpoint approval when required.

Evidence is accepted only per named tool. Duplicate evidence for the same tool fails closed, unknown tool IDs fail closed, and accepted evidence for one tool does not bypass platform blockers or remaining tools' blockers.

The shared platform blocker is also evidence-driven. It clears only when a staging or production evidence packet proves:

- `tool_cost_events` migration deployment;
- backend service-role event writes;
- authenticated RLS member reads;
- idempotent replay;
- wallet settlement behavior;
- Stripe boundary behavior;
- monitoring;
- billing QA;
- deployment, security, storage, legal, and support approvals.

Partial platform evidence stays blocked and names the missing requirement.

Top-level beta checklist blockers can also be cleared only with explicit checklist evidence. The evidence must name a known checklist item, include a source ID, carry `passed` or `warning` status, and include notes. Duplicate checklist evidence fails closed.

## Backend Evaluation API

The backend now exposes an authenticated, mock-safe evaluation surface for this gate:

- `GET /v1/beta-readiness` returns the default source-of-truth readiness report.
- `POST /v1/beta-readiness/evaluate` accepts structured evidence for checklist items, per-tool accepted execution, platform billing/deployment readiness, and human approvals, then returns the computed report.
- `POST /v1/beta-readiness/evidence` records a sanitized evidence packet idempotently and returns the computed report.
- `GET /v1/beta-readiness/evidence` lists stored evidence packets, the merged evidence view, and the computed report.
- `GET /v1/beta-readiness/operator-status` returns a read-only backend operator status summary. Without `workspaceId`, it reports default source-truth blocker state. With `workspaceId`, it reads stored evidence and returns evidence count, scoped blocked actions, allowed forward-progress scopes, remaining go/no-go gaps, and next actions without writing evidence, running tools, or enabling beta/production.
- `POST /v1/beta-readiness/evidence/core-real-check` runs only the bounded core command/import/package-metadata checks and records accepted evidence only for tools that actually pass those checks.
- `GET /v1/beta-readiness/platform-preflight` returns a read-only local platform evidence preflight. It verifies source artifacts such as migration files and backend route/store wiring, names the staging/production-only evidence still missing, and does not write evidence or open beta/production gates.
- `POST /v1/beta-readiness/platform-billing-qa` runs an idempotent platform billing QA harness. In mock mode it records a controlled in-memory tool-cost event, replays the same idempotency key, reads the project summary, confirms wallet settlement and Stripe remain separate, invokes the explicit mock wallet-settlement skeleton, and names the staging/production evidence still missing. In non-mock mode, persistent platform billing QA fails closed before writes until a transactional wallet-settlement RPC exists.
- `POST /v1/beta-readiness/platform-deployed-evidence/verify` validates deployed platform probe observations against the backend verifier. By default it is report-only. Recording requires auth, idempotency, `recordEvidence: true`, `confirmRecordEvidence: true`, every deployed probe passing, and every owner approval present. The route records only the resulting platform evidence packet and does not run probes, call Stripe, execute tools, process media, enable external beta, or mark production ready.
- `POST /v1/beta-readiness/platform-deployed-evidence/probe` runs the backend-owned Supabase deployed probe transport. It can read deployed `tool_cost_events` and `beta_readiness_evidence_packets`, optionally write controlled backend-only probe evidence when `allowPersistentProbeWrites=true`, verify idempotent replay, and call the wallet-settlement RPC only when an approved staging fixture event ID is supplied. RLS/member readback, Stripe-boundary, monitoring, and billing-QA evidence remain explicit deployed attestations. Recording still requires a complete packet plus `recordEvidence: true` and `confirmRecordEvidence: true`.
- `npm run beta:readiness:operator-status` is the combined no-network operator command for the current evidence lane. It runs the tool-evidence and platform-evidence preflight builders, reports current blocker counts, names missing input groups, and prints the next commands in order without contacting staging, running tool checks, writing evidence, or opening beta/production.
- `npm run beta:readiness:blocker-ledger` prints a no-network machine-readable blocker ledger. It expands tool, platform, checklist, and go/no-go blockers into stable row keys, missing evidence, blocked action scope, safe forward-progress scope, clearance type, and the next safe action. Duplicate row keys are surfaced before teams create duplicate PRs or duplicate evidence packets. The ledger does not run tools, write evidence, process media, enable beta, or enable production.
- `npm run beta:readiness:operator-status-api` is the read-only deployed backend status readback command. It calls `GET /v1/beta-readiness/operator-status`, sends the bearer token only in the authorization header, can include `REEDITPRO_BETA_STATUS_WORKSPACE_ID`, and fails closed when `REEDITPRO_BETA_STATUS_REQUIRE_EXTERNAL_BETA_READY`, `REEDITPRO_BETA_STATUS_REQUIRE_REAL_USER_MEDIA_BETA_READY`, or `REEDITPRO_BETA_STATUS_REQUIRE_PAID_PRODUCTION_READY` is requested but the backend status is not ready.
- `npm run beta:readiness:external-beta-evidence-collector` is the all-up deployed evidence collector for the external-beta gate. It requires `REEDITPRO_BETA_EXTERNAL_CONFIRM_EVIDENCE_SEQUENCE=true`, runs the local accepted tools collector, records deployed platform evidence, records launch approval evidence, then performs the final read-only operator-status check with external-beta readiness required by default. It does not enable real-user-media beta, paid production, provider calls, worker dispatch, direct Supabase writes, public artifacts, or product runtime execution.
- `npm run beta:readiness:paid-production-evidence-collector` is the final all-up evidence collector for paid-production readiness. It requires `REEDITPRO_BETA_PAID_PRODUCTION_CONFIRM_EVIDENCE_SEQUENCE=true`, runs the external-beta evidence collector, runs the real-user-media and paid-production scope approval sequence, then performs a final read-only operator-status check requiring external beta, real-user-media beta, and paid production readiness. It is still evidence collection/readback only and does not run user media, charge users, dispatch workers, call providers, deploy production, write Supabase directly, or create public artifacts.
- `npm run beta:readiness:launch-approval-evidence-preflight` is the no-network operator preflight for top-level external-beta launch approvals. It checks API/auth/workspace/idempotency/source SHA inputs, model/license and deployment checklist evidence, deployment/security/storage/model/legal/monitoring/support approval confirmations, and secret-like evidence text before any deployed backend call is made.
- `npm run beta:readiness:launch-approval-evidence` records only the external-beta launch approval packet through `POST /v1/beta-readiness/evidence`. It clears the model/license and deployment checklist blockers and records named owner approvals only when every explicit confirmation/evidence note is present. It intentionally rejects real-user-media beta and paid-production approvals; those remain separate later gates.
- `npm run beta:readiness:scope-approval-evidence-preflight` is the no-network operator preflight for later real-user-media beta or paid-production approval evidence. It accepts only one mode at a time: `real_user_media_beta` requires external beta to be ready before recording, and `paid_production` requires real-user-media beta to be ready before recording.
- `npm run beta:readiness:scope-approval-evidence` first reads `GET /v1/beta-readiness/operator-status` for the deployed workspace, then records exactly one scope approval packet through `POST /v1/beta-readiness/evidence` only if the prerequisite gate is already ready. It does not run tools, process media, charge users, deploy production, or bypass final go/no-go readback.
- `npm run beta:readiness:scope-approval-sequence` is the all-up deployed scope approval evidence sequence for the final beta/production approval gates. It requires explicit sequence, real-user-media beta, and paid-production confirmations, posts real-user-media beta approval only after external beta readback is ready, posts paid-production approval only after real-user-media beta readback is ready, then performs a final operator-status readback with paid-production readiness required by default. It does not run tools, process media, charge users, dispatch workers, call providers, write Supabase directly, deploy production, or create public artifacts.
- `npm run beta:tools:core-real-check-preview` is the local no-write preview for reducing per-tool blockers before staging evidence is recorded. It runs only the bounded command/import/package-metadata checks already used by the core real-check evidence builder, scopes the request to `REEDITPRO_BETA_TOOLS_PREVIEW_TOOL_IDS` when supplied, rejects unknown tools and secret-like notes, requires explicit production/product-ready acceptance confirmations, and fails closed when no tool evidence is accepted. It does not call the deployed backend, write evidence, process media, run Docker, call providers, write Supabase, enable beta, or enable production.
- `npm run beta:tools:core-real-check-preview:hydrated` is the local no-write preview for the ignored core Python readiness virtualenv. It requires `.reeditpro-tool-readiness-python/bin/python` or an explicit `REEDITPRO_READINESS_PYTHON_BIN`, sets that Python only for the preview run, and fails closed if the hydrated Python is missing. It is intended to be run after `npm run tools:readiness:install-core-python` so Python-backed core tools can move from missing/not-installed preview status to bounded import evidence without writing backend records.
- `npm run beta:tools:local-accepted-evidence-bundle` combines the hydrated core real-check preview with the synthetic-only libass burn-in QA preflight into one local no-write evidence bundle. It requires explicit production-readiness and product-ready local OSS acceptance confirmations, fails closed if either core or libass accepted evidence is missing, reports the locally accepted tool IDs, preserves the historical PR #73 libass context as non-current duplicate context, and names the remaining deployed evidence, platform evidence, launch approval, and operator-status readback gates. It does not call the deployed backend, write evidence, process user media, write Supabase, run workers, enable external beta, or enable production.
- `npm run beta:tools:local-accepted-evidence-collector` is the deployed recording handoff for that local bundle. It runs the local bundle first, posts core accepted evidence to `/v1/beta-readiness/evidence/core-real-check`, posts the accepted libass evidence packet to `/v1/beta-readiness/evidence`, then reads `/v1/beta-readiness/operator-status?workspaceId=...` and fails closed unless the readback product-ready local OSS count meets `REEDITPRO_BETA_TOOLS_LOCAL_BUNDLE_REQUIRED_PRODUCT_READY_LOCAL_OSS_COUNT`. It requires separate core and libass idempotency keys, explicit operator readback confirmation, API auth, source SHA, and product-ready acceptance confirmations. It records tool evidence only; it does not record platform evidence, record launch approvals, process user media, write Supabase directly, run providers/workers, enable external beta, or enable production.
- `npm run beta:tools:libass-container-proof-preflight` is the local no-write proof lane for the remaining libass subtitle-filter warning. It runs only `ffmpeg -hide_banner -filters` directly or through `docker run --rm --network none <approved-image> ffmpeg -hide_banner -filters`, emits an accepted `libass` evidence packet preview only when `ass` or `subtitles` filters are present, and refuses product-ready local OSS acceptance because final caption burn-in, font packaging, safe-zone QA, and FFmpeg LGPL/commercial review remain separate gates.
- `npm run beta:tools:libass-synthetic-burnin-qa-preflight` is the local synthetic-only QA lane after filter proof. It creates temp synthetic video and ASS captions, runs one bounded libass burn-in/probe with explicit `subtitles=filename=...` filter syntax, records checksums/sizes/duration and safe ASS style status, deletes temp artifacts, and emits a `libass` product-ready local OSS evidence preview only when explicit production and product-ready confirmations are supplied. Failed burn-in attempts preserve the actual command/stderr record so a missing `ass`/`subtitles` filter points to an approved host/image blocker instead of an opaque internal failure. It records open PR #73 as historical activation/private-media context, not as a current-source duplicate to merge.
- `npm run beta:tools:libass-synthetic-burnin-qa-evidence-preflight` is the no-network staging-recording preflight for accepted libass QA evidence. It checks deployed API/auth/idempotency inputs, source SHA, QA mode/container inputs, production/product-ready confirmations, require-accepted/readback guards, and secret-like notes before any backend call.
- `npm run beta:tools:libass-synthetic-burnin-qa-evidence` reruns the synthetic-only libass QA preflight locally and records only its accepted `libass` evidence packet through `POST /v1/beta-readiness/evidence`. It fails closed if QA does not produce evidence or deployed readback omits `libass`; it still does not run user media, write Supabase directly, enable beta, enable paid production, or clear platform/launch gates by itself.
- `docs/beta-readiness/local-accepted-evidence-bundle/2026-06-27-local-accepted-evidence-bundle.md` records the current local no-write evidence snapshot: 14 locally accepted tools, including the hydrated core command/import/package checks plus no-network synthetic libass burn-in QA against the approved local staging image. This snapshot is ready for deployed staging evidence recording, but it is not deployed backend evidence and does not enable external beta, real-user-media beta, paid production, provider calls, worker dispatch, Supabase/GCS writes, public artifacts, signed URLs, or product-ready status for every registry tool.
- `docs/beta-readiness/core-python-readiness-rerun/2026-06-28-post-1420-core-python-readiness-rerun.md` records the post-PR #1420 current-source core rerun at `9bda4642fefa82714d30868f1625a6155f3363b3`: hydrated Python imports passed and the local no-write core preview accepted 13 tools (`ffmpeg`, `ffprobe`, `pyav`, `opentimelineio`, `hyperframe`, `remotion`, `sharp`, `duckdb`, `polars`, `pyscenedetect`, `opencv`, `opencolorio`, and `openimageio`). `libass` remains the next current-source bounded synthetic subtitle QA refresh before a 14-tool local bundle is reissued at the latest tools SHA.
- `docs/beta-readiness/core-python-readiness-rerun/2026-06-28-core-python-cold-import-timeout-hardening.md` records the follow-up hardening after a post-merge cold PySceneDetect import timeout reproduced immediately after venv hydration. The core readiness runner now uses one resolved Python command per real-check run and a 45 second bounded timeout so installed native Python packages are not falsely classified as missing during cold import startup.
- `docs/beta-readiness/local-accepted-evidence-bundle/2026-06-28-current-source-local-accepted-evidence-bundle.md` records the current-source local no-write evidence bundle after that hardening at `a1943442b794f7ae5216adf501a617a9f4478185`: 14 locally accepted tools are ready for deployed staging evidence recording. This bundle also records the scoped blocker policy: the remaining blockers close only external beta, real-user-media beta, paid production, provider calls, worker dispatch, Supabase/GCS writes, public artifact delivery, and signed URL delivery; they do not block source-truth review, local no-write preview, deployed evidence preflight, owner approval collection, diagnostics, QA review, or monitoring and rollback planning.
- `npm run beta:readiness:api-deployment-preflight` is the no-network staging API deployment handoff check before deployed evidence collection. It verifies the staging-only Cloud Run API service/image/source SHA/base URL/secret-binding inputs are coherent and that no production, unauthenticated, runtime tool, media, Supabase/GCS, or evidence-write action is being performed by the preflight.
- `.github/workflows/beta-readiness-api-staging-input-discovery.yml` is the guarded read-only default-branch workflow for proving staging API deploy inputs before deploy. The latest exact-input source-truth run [28309751101](https://github.com/yuzastudio6-cyber/Reedkt/actions/runs/28309751101) authenticated through Workload Identity and setup-gcloud, confirmed exact deployer service-account describe works, then failed on `artifactregistry.repositories.get` for `projects/reeditpro/locations/us-central1/repositories/reeditpro-staging-workers` and `NOT_FOUND` for `reeditpro-api-staging@reeditpro.iam.gserviceaccount.com`; Cloud Run service listing still succeeds. The post-current-source local bundle owner prerequisite audit rerun [28321041675](https://github.com/yuzastudio6-cyber/Reedkt/actions/runs/28321041675) confirmed the same owner-side gaps remain: Artifact Registry `get`/`uploadArtifacts`, the exact staging runtime service account, deployer act-as-runtime, and fixed staging Secret Manager metadata describe. This blocks only staging API deploy until owner Artifact Registry repository access and a staging API runtime service account are proven; it does not block source reviews, diagnostics, owner approvals, read-only discovery, or other bounded blocker-reduction work.
- `npm run beta:readiness:owner-command-handoff` is the no-mutation owner handoff for the current staging API deploy-input blocker. It prints the exact higher-privilege owner command plan and shell-script template, requires owner-supplied fixed staging API secret names through `REEDITPRO_FIXED_STAGING_API_SECRET_NAMES_CSV`, omits `roles/run.admin`, does not print secret values, and does not run `gcloud`, Docker, deploy, tools, media processing, Supabase/GCS, beta, or production. After an owner applies it outside Codex, rerun the read-only owner prerequisite audit before exact input discovery or deploy.
- `.github/workflows/beta-readiness-api-staging-deploy.yml` is the guarded manual staging API deployment path for the deployed evidence gap. It is discoverable from default branch `codex/reeditpro-web-ui-shell` after PR #1360 and must be dispatched from that branch while fetching `source_ref=codex/sound-music-audio-1abc-checkpoint`. It is `workflow_dispatch` only, requires `DEPLOY_STAGING_BETA_READINESS_API`, locks Cloud Run to `reeditpro-api-staging`, rejects `latest` image tags, uses Workload Identity, binds Secret Manager names only, and keeps `--no-allow-unauthenticated`. It does not run evidence collectors, enable beta, enable paid production, process media, or prove product readiness by itself.
- `npm run beta:readiness:deployed-evidence-input-manifest` is the no-network input manifest for the next deployed evidence gate. It ties the 14-tool local accepted snapshot to an operator-supplied deployed source SHA, fixed core tool list, libass image/mode, required product-ready local OSS count, staging-only platform packet, external-beta-only launch approvals, and operator-provided evidence notes/idempotency keys before `npm run beta:readiness:external-beta-evidence-collector` may be run.
- `npm run beta:tools:core-real-check-evidence-preflight` is the no-network operator preflight for per-tool accepted evidence. It checks staging API/auth/workspace/idempotency presence, source SHA, requested tool IDs, explicit production/product-ready acceptance confirmations, require-accepted-evidence mode, and secret-like notes before any deployed backend call is made. It prints a sanitized readiness report and does not run tool checks or write evidence.
- `npm run beta:tools:core-real-check-evidence` is the operator CLI for the bounded per-tool real-check route. It posts to `/v1/beta-readiness/evidence/core-real-check`, carries only workspace/project/source/tool IDs and non-secret notes, sends the bearer token only in the authorization header, and requires explicit confirmation before requesting production-readiness or product-ready local OSS acceptance. The backend still runs only safe command/import/package metadata checks and records evidence only for tools that actually pass.
- `npm run beta:platform:staging-evidence-preflight` is the no-network operator preflight for the staging collector. It checks only env-var presence, explicit owner approvals, redacted deployed attestations, wallet-settlement fixture ID, record confirmations, source SHA, and secret-like evidence text before any deployed backend call is made. It prints a sanitized readiness report, never prints bearer tokens or service-role secrets, and does not contact Supabase, execute tools, process media, write evidence, enable beta, or enable production.
- `npm run beta:platform:staging-evidence-probe` is the operator CLI for the deployed probe route. It reads staging API URL, bearer token, workspace/project IDs, idempotency key, owner approvals, fixture event ID, and attestation notes from `REEDITPRO_BETA_PLATFORM_*` environment variables, posts to the deployed backend, and prints only a sanitized summary. It does not contain credentials, does not print bearer tokens, and fails closed when `REEDITPRO_BETA_PLATFORM_REQUIRE_READY=true` and the evidence packet is incomplete.
- `POST /v1/tool-costs/events/:toolCostEventId/settle` records an idempotent mock wallet-settlement ledger effect for a tool-cost event in local/mock mode. It is backend-owned, requires auth and idempotency, does not call Stripe, does not include ReEditPro service/edit fees, and in non-mock runtime calls the `settle_tool_cost_event` RPC from the wallet-settlement migration. Missing migration/RPC support fails closed with `TOOL_COST_BACKEND_REQUIRED`.
- `npm run smoke:tool-cost-wallet-settlement:service-role` verifies the non-mock service-role settlement path with a controlled fake Supabase admin client. It checks replay lookup, RPC function/parameter shape, persistent row mapping, Stripe isolation, service-fee exclusion, and fail-closed behavior when the RPC is missing. This smoke does not touch remote Supabase and does not prove deployed service-role credentials.
- `npm run smoke:tool-cost-wallet-settlement:sql` applies the wallet-settlement migration to a disposable local Postgres database with minimal prerequisite tables, settles a billable tool-cost event, replays the same idempotency key without double-spending, settles a provider failure as non-billable, and verifies RLS/policy/source metadata. This is local SQL evidence only; it does not touch remote Supabase, Stripe, providers, media tools, beta, or production.
- `npm run smoke:beta-readiness-operator-status` verifies the combined CLI operator status report for complete and missing input cases, secret-safe output, current blocker counts, evidence-driven policy, and next-command sequencing without touching a deployed backend. `npm run smoke:beta-readiness-operator-status-api` verifies the deployed readback CLI uses GET, keeps bearer tokens out of summaries, supports stored-workspace status, and fails closed when required readiness is absent. `npm run smoke:beta-readiness-external-beta-evidence-collector` verifies the all-up evidence collector sequencing, idempotency keys, secret-safe summaries, final external-beta readback requirement, and preserved real-user-media/paid-production blockers without touching a remote backend. `npm run smoke:beta-readiness-api` verifies the deployed backend operator-status route stays read-only and reflects stored workspace evidence.
- `npm run smoke:beta-readiness-blocker-ledger` verifies the blocker ledger has no duplicate row keys, every blocker has missing-evidence text plus a next safe action, blanket blockers remain disallowed, default product-ready local OSS remains `0`, complete accepted evidence can clear the ledger, and duplicate accepted tool evidence still fails closed.
- `npm run smoke:scoped-blocker-forward-progress-policy` verifies the repo-level hard rule that blockers may close only named unsafe actions and must keep named safe forward-progress lanes open. It checks the tool readiness report, blocker ledger, backend operator status, and production hardening report against the shared scoped-blocker policy.
- `npm run smoke:beta-readiness-paid-production-evidence-collector` verifies the final all-up paid-production collector sequencing, nested external-beta and scope collectors, separate idempotency keys, final paid-production readback requirement, secret-safe summaries, and no-user-media/no-charge/no-worker/no-provider boundaries without touching a remote backend.
- `npm run smoke:beta-readiness-launch-approval-evidence-preflight` verifies launch approval evidence preflight fail-closed behavior for missing inputs, missing owner approvals, missing evidence notes, wider-scope rejection, and secret-like evidence. `npm run smoke:beta-readiness-launch-approval-evidence-cli` verifies the recording CLI request shape, idempotency/auth headers, checklist/approval packet shape, real-user-media/paid-production rejection, require-ready fail-closed behavior, and sanitized summary output without touching a remote backend.
- `npm run smoke:beta-readiness-scope-approval-evidence-preflight` verifies scope approval preflight fail-closed behavior for invalid modes, missing inputs, missing confirmations/evidence notes, mixed-scope rejection, and secret-like evidence. `npm run smoke:beta-readiness-scope-approval-evidence-cli` verifies prerequisite status readback, real-user-media and paid-production packet shapes, idempotency/auth headers, fail-closed prerequisites, target-ready checks, and sanitized output without touching a remote backend.
- `npm run smoke:beta-readiness-scope-approval-sequence` verifies the ordered real-user-media beta then paid-production approval evidence sequence, separate idempotency keys, prerequisite readback gates, final paid-production readback requirement, secret-safe summaries, and no-tool/no-media/no-charge boundaries without touching a remote backend.
- `npm run smoke:beta-tools-core-real-check-evidence-cli` verifies the per-tool core real-check evidence CLI request shape, idempotency/auth headers, explicit acceptance confirmations, unknown-tool rejection, secret-like note rejection, require-accepted-evidence failure behavior, and sanitized summary output without touching a deployed backend.
- `npm run smoke:beta-tools-core-real-check-evidence-preflight` verifies the no-network per-tool evidence preflight for complete operator inputs, missing configuration reporting, confirmation-gap reporting, invalid tool rejection, and secret-like note rejection.
- `npm run smoke:beta-platform-rls-readback:sql` applies the tool-cost event, beta evidence, and wallet-settlement migrations to a disposable local Postgres database with Supabase-like `authenticated` role and `auth.uid()` behavior. It verifies a workspace member can read only scoped tool-cost and wallet-settlement rows, a non-member reads none, beta readiness evidence remains backend-only, and authenticated inserts stay denied. This is local RLS evidence only; deployed staging/production readback is still required.
- `npm run smoke:beta-platform-monitoring-catalog` verifies source templates for tool-cost event writes, idempotent replay, persistent write failures, wallet settlement, RLS readback, billing QA missing evidence, and Stripe-boundary violations. These are monitoring templates only; no dashboard or alert is deployed by the smoke.
- `npm run smoke:beta-platform-stripe-boundary` verifies the tool-cost and beta billing surfaces do not depend on Stripe, import Stripe, instantiate Stripe, call Stripe APIs, or lose the `stripeCallAttempted: false` and `serviceFeeIncluded: false` audit markers. This is source evidence only; billing-owner approval remains required.
- `npm run smoke:beta-platform-evidence-manifest` verifies the platform blocker is evidence-driven rather than a permanent hard wall. It maps each remaining shared platform requirement to existing local proof commands, source files, deployed staging/production evidence, owner approvals, and a next safe action. The smoke proves complete platform evidence can clear the shared platform blocker while default/local-only evidence still keeps external beta and production closed.
- `npm run smoke:beta-platform-deployed-evidence-verifier` verifies the backend verifier that will turn real staging/production probe results into a platform evidence packet. The verifier fails closed when any deployed probe is missing, any owner approval is absent, or secret-like material appears in notes. Even a complete platform packet clears only the shared platform blocker; per-tool accepted execution evidence and launch approvals are still separate gates.
- `npm run smoke:beta-platform-deployed-evidence-probes` verifies the adapter that maps deployed migration, service-role, RLS, idempotency, wallet-settlement, Stripe-boundary, monitoring, and billing-QA observations into the verifier's probe contract. A failed transport observation keeps the platform blocker closed.
- `npm run smoke:beta-platform-supabase-deployed-evidence-transport` verifies the backend Supabase transport with a fake service-role client. It proves deployed-table read probes, controlled evidence write/replay probes, wallet-settlement RPC call shape, missing-admin fail-closed behavior, and persistent-write confirmation gating without touching remote Supabase or enabling beta/production.
- `npm run smoke:beta-platform-staging-evidence-collector-cli` verifies the staging evidence collector CLI request shape, secret omission from request bodies/summaries, production-confirmation guard, require-ready failure behavior, and sanitized summary output without touching a remote backend.
- `npm run smoke:beta-platform-staging-evidence-preflight` verifies the no-network preflight for complete staging operator inputs, missing approval/evidence reporting, production rejection for the staging lane, and secret-like evidence rejection.

In mock mode, stored evidence is in-memory only. In non-mock mode, evidence writes require the `beta_readiness_evidence_packets` migration and backend service-role path; missing persistence fails closed instead of silently enabling beta. These routes do not reserve or spend credits, call providers, run tools, enable beta, or mark production ready by themselves. They make the gate executable from backend callers, so blocker state is computed from explicit evidence instead of a hidden hardcoded wall. Unknown tools, unknown checklist items, duplicate downstream evidence, and secret-like payloads fail closed.

Core real-check evidence is deliberately narrow: it may run version checks, Python imports, and Node package metadata resolution already defined by the production readiness specs. It does not process media, render/export, run providers, download models, run Docker, or accept missing tools. A tool is counted as product-ready local OSS only when the request explicitly accepts production readiness and product-ready local OSS for passed checks. Operators should first use `npm run beta:tools:core-real-check-evidence-preflight`, then use `npm run beta:tools:core-real-check-evidence` against staging after dependency/tool installation is complete, with `REEDITPRO_BETA_TOOLS_CONFIRM_PRODUCTION_READINESS_ACCEPTANCE=true` and `REEDITPRO_BETA_TOOLS_CONFIRM_PRODUCT_READY_LOCAL_OSS_ACCEPTANCE=true` only when the owner intends those passed checks to count as accepted evidence.

For local core Python readiness proof, run `npm run tools:readiness:install-core-python` to hydrate the ignored `.reeditpro-tool-readiness-python/` virtualenv from `docker/prod/tool-readiness-worker/requirements.readiness.txt`, then run `npm run smoke:prod-core-python-readiness` and `npm run beta:tools:core-real-check-preview:hydrated` with the same `REEDITPRO_BETA_TOOLS_PREVIEW_*` acceptance inputs. This imports only the safe core Python packages (`av`, `scenedetect`, `cv2`, `duckdb`, `polars`, `opentimelineio`, `PyOpenColorIO`, and `OpenImageIO`) and must not process media, transform images, run color pipelines, run Docker, call providers, or enable beta/production.

## Evidence-Driven Policy

The beta go/no-go policy is not a permanent hardcoded block. External beta, real-user-media beta, and paid production are computed from supplied evidence and approvals:

- dry-run and safety/cost documentation must pass;
- production readiness must not be blocked;
- required checklist items must not be blocked;
- deployment, security, storage/privacy, model/license, legal, monitoring, and support approvals must be present;
- real-user-media beta and paid production require their own explicit approvals.

The default repo state remains blocked because those approvals and real execution proofs are not present yet. Local service-role path, SQL, monitoring-template, and Stripe-boundary source proof can reduce wallet-settlement, service-role, RLS/readback, monitoring, and Stripe-boundary blockers from "unimplemented source" to "needs deployed staging/prod/owner evidence", but it does not clear the platform gate by itself.

Every blocked state must remain paired with an unblock path. If a tool, checklist item, or platform gate is blocked, the report must describe the missing evidence and the next safe lane that can collect it. This policy keeps beta and production guarded, but prevents blockers from becoming intentional permanent walls when a smaller source-review, local-proof, diagnostics, or QA step can safely move the tool lane forward.

The gate blocks only the unsafe action it is protecting. A tool blocker can block external beta execution for that tool without blocking source classification, package approval, local command/import proof, cost-metering integration, diagnostics, QA review, or rollback planning. A platform blocker can block billable external beta without blocking local source preflights, persistent-store skeletons, migration reviews, monitoring plans, wallet-settlement tests, or owner approval packets. Reports should prefer precise next lanes over broad stop language.

The executable report now exposes this as source-of-truth metadata:

- `blockerPolicy: evidence_driven_block_unsafe_actions_only`
- `blockerForwardProgressPolicy.intentionalBlanketBlocksAllowed: false`
- `blockerForwardProgressPolicy.blockerScope: named_unsafe_action_only`
- `blockerForwardProgressPolicy.safeForwardProgressRequired: true`
- `blockerForwardProgressPolicy.nextSafeActionRequiredForBlockers: true`
- `safeBlockerReductionAllowed: true`
- `blockedActionScope`, which must name only the unsafe beta/production actions currently closed.
- `allowedForwardProgressScopes`, which names bounded source review, local proof, safe blocker-reduction preview, diagnostics, QA, deployment preflight, owner approval, rollback, monitoring, and support planning lanes that can continue while beta/production execution remains closed.

These fields are intentionally separate from `externalBetaToolExecutionAllowed` and `productionToolExecutionAllowed`. They let automation and agents keep building the next bounded unblock step while still refusing live beta/production execution until the required evidence exists. A blocker report must not be interpreted as "stop all work"; it must be interpreted as "stop the named unsafe action and continue only the named evidence-reduction lanes."

The blocker ledger also repeats that policy on every row:

- `blockerMode: scoped_unsafe_action_only`
- `blocksSafeForwardProgress: false`
- `clearanceType`, one of source review, bounded local proof, safe preview, diagnostics/QA, deployed platform evidence, owner approval, or rollback/monitoring planning.

These row fields are the hard rule for future automation. A blocked row is a clearance work item, not an intentional blanket freeze. If a row blocks beta or production but does not expose safe blocker-reduction metadata, the correct fix is to repair the blocker metadata first, then continue the smallest safe lane that can retire the named missing evidence.

The platform evidence manifest makes that policy executable. Each shared platform blocker must name the local source/runtime proof already available, the deployed or owner evidence still missing, and the next safe lane that can gather it. A missing platform requirement can block live external beta without blocking source-of-truth review, local SQL proof, service-role call-shape proof, monitoring-template proof, Stripe-boundary proof, billing QA planning, or named owner approval collection.

The deployed platform evidence verifier is the handoff point for the next safe lane. It is designed for a future staging transport to supply verified migration, service-role, RLS, idempotency, wallet-settlement, Stripe-boundary, monitoring, and billing-QA probe results. It does not write the evidence packet by itself and does not carry secrets; recording remains behind the authenticated/idempotent backend evidence route.

The deployed probe adapter and Supabase transport are the tested seam for staging evidence collection. They let real Supabase/readback/monitoring checks plug into the evidence verifier without changing the gate logic, and keep a single failed observation from producing a platform evidence packet. The Supabase transport can do backend-owned table, write, replay, and wallet-settlement probes; it still requires explicit staging attestations for RLS/member readback, Stripe-boundary, monitoring deployment, and billing QA.

The staging evidence collector CLI is the handoff from source readiness to human-run staging validation. Operators should first run `npm run beta:readiness:operator-status`, then run `npm run beta:tools:core-real-check-preview` locally to prove a blocker can shrink without writing backend evidence. For Python-backed core tools, run `npm run tools:readiness:install-core-python` and `npm run beta:tools:core-real-check-preview:hydrated` before deployed evidence recording. After core and libass local evidence pass, run `npm run beta:tools:local-accepted-evidence-bundle` to confirm the current local accepted tool set and the remaining staging gates in one report. Then run `npm run beta:tools:local-accepted-evidence-collector` against deployed staging to record the core and libass evidence and verify operator-status readback. Follow with `npm run beta:platform:staging-evidence-preflight` and `npm run beta:readiness:launch-approval-evidence-preflight`. After those preflights pass, operators can either record each evidence packet separately or run `npm run beta:readiness:external-beta-evidence-collector` to sequence tool evidence, deployed platform evidence, launch approval evidence, and final operator-status readback in one fail-closed command. A successful external-beta evidence run is still evidence collection, not beta launch. Only after external beta is ready may operators run `beta:readiness:scope-approval-evidence-preflight` in `real_user_media_beta` mode; only after real-user-media beta is ready may they run it in `paid_production` mode. When both later approvals and evidence notes are available, `npm run beta:readiness:scope-approval-sequence` can record both scope approvals in order and require final paid-production operator-status readback. When all external-beta, real-user-media beta, and paid-production approvals are available, `npm run beta:readiness:paid-production-evidence-collector` can sequence the entire evidence/readback path and require final paid-production readiness. Product-ready claims remain closed until the separate approvals and final go/no-go review are present.

## Why This Does Not Flip Beta On

The current evidence is dry-run and source-of-truth only. External beta needs real bounded runtime checks, deployment/storage/security approvals, deployed and verified cost persistence, model/license approvals, monitoring, rollback, and support readiness.

The gate is intentionally strict but no longer uses one permanent per-tool billing wall, one permanent platform billing wall, or a permanent product-ready `false`. If any tool lacks coverage, readiness evidence, model approval, or product-ready acceptance, external beta and paid production remain blocked for that tool. If shared billing deployment/QA is unverified, external beta and paid production remain blocked at the platform level.

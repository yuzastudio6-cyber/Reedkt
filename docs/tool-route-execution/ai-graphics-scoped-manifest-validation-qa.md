# AI Graphics Scoped Manifest Validation QA

Decision: `tool_route_ai_graphics_metadata_local_fixture_validation_qa_passed_with_warnings`

Scoped manifest validation from PR #464 is accepted with warnings. QA confirms fixture validation requires placeholder scoped tool-call manifest refs and accepted AI graphics capability ids for all 13 accepted tools.

Signed URLs are not source of truth. The accepted QA boundary requires approved plan snapshot placeholders, scoped tool-call manifest placeholders, private artifact refs, checksum placeholders, fail-closed behavior, and no-execution proof.

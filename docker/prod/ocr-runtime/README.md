# Phase 37C OCR Runtime Local Docker Fallback

This image is for a guarded local fallback only when macOS/Python compatibility blocks the isolated venv runtime.

It must not be pushed, deployed, attached to Cloud Run, or used with real media. Build only with `REEDITPRO_CONFIRM_OCR_RUNTIME_DOCKER_BUILD=true`.

# AI Graphics Local Fixture Scoped Manifest Validation Evidence

Decision: `tool_route_ai_graphics_metadata_local_fixture_validation_passed_with_warnings`

Scoped manifest validation passed with warnings for all 13 accepted tools. Each fixture family must preserve a placeholder scoped tool-call manifest reference and accepted AI graphics capability id before any later owner reviews consider Tool Route handoff.

Signed URLs are not source of truth. The scoped manifest policy requires placeholder approved plan snapshot, placeholder scoped tool-call manifest, private artifact refs, checksum placeholders, fail-closed behavior, and no-execution proof.

# Route And Ranking Contract Validation

The dry-run diagnostics verify three disabled route IDs and 16 callable worker contracts. Ranking metadata covers every Track B tool exactly once and remains dry-run-only with execution disabled.

Default ranking sequence: `ffprobe`, `mediainfo`, `exiftool`, `duckdb`, `polars_nodejs_polars`, `sharp_libvips`, `opencolorio`, `openimageio`, `imagemagick`, `opencv`, `pyav`, `pyscenedetect`, `tesseract`, `paddlepaddle`, `paddleocr`, `ffmpeg`.

No direct runtime route or worker dispatch is enabled.

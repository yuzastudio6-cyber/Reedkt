# Edit Level Client Transition Plan

RP-EDITLEVEL-03 prepares the client boundary without making it visible in the product. The browser-safe client can call mock transport methods for profile data, source-aware normalization, recommendation fixtures, selection lifecycle, summaries, readiness, logs, and repository summary.

RP-EDITLEVEL-04 adds visible UI cards and recommendations in available setup/chat/brief surfaces. The selection remains mock/local and does not change `ChatNativeEditor` runtime behavior, production persistence, or planner/tool routing.

Future production transition must keep backend-only persistence behind reviewed routes and approved plan/credit gates. Client calls must not imply provider execution, media processing, render budget execution, or credit spend before approval.

RP-EDITLEVEL-05 is complete as a mock/local tool capability router.

Recommended next prompt: `RP-EDITLEVEL-06 - Level-Aware Source Video Understanding Routing`.

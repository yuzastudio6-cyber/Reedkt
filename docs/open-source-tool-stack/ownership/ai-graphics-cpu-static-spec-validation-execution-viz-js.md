# CPU Static Spec Validation Execution: viz_js

Decision: `blocked_pending_cpu_static_dependency_install_from_lock`

Tool: `viz_js`

Package: `@viz-js/viz`

Canonical proof level preserved: `canonical_merged_package_import_static_fixture_proof`

The approved future fixture was an import/API and small DOT graph metadata contract check only, with no public artifact. The execution did not run because `@viz-js/viz` is absent from `package.json` and `package-lock.json`.

No actual tool execution, browser/WebGL/canvas runtime, public artifact, signed URL, beta, or production unlock occurred.

# Canonicalization Owner Approval QA Planning-Only Policy

Decision: `ai_graphics_tool_capability_study_canonical_agent_routing_canonicalization_owner_approval_qa_passed_with_warnings`.

Agents may select AI graphics tools for planning/study metadata only. Agents must not execute tools, routes, workers, providers/models, browser/WebGL/canvas runtime, GPU/model runtime, storage actions, signed URLs, public artifacts, beta, or production workflows.

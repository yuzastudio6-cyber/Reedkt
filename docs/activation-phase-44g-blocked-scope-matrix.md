# Phase 44G Blocked Scope Matrix

Still blocked:

- route execution
- worker execution
- tool execution
- raw chat execution
- arbitrary subprocess or shell execution
- arbitrary paths or arbitrary GCS prefixes
- media/audio/OCR/VLM/model runtime
- DeepFilterNet, Signalsmith, Demucs runtime
- provider calls and web search provider calls
- Docker, Cloud Build, Cloud Run, GPU jobs, GCP/IAM mutation
- public artifacts and public output
- broad media and arbitrary media
- product-wide internal beta, external beta, paid production, production
- Track A runtime/visual/render stack

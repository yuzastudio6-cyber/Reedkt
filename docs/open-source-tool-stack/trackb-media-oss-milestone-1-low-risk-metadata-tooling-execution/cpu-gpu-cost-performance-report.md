# CPU/GPU Cost Performance Report

Milestone 1 is CPU-only/default. GPU is not required or used. Future runtime target is a CPU Cloud Run Job or CPU worker image after packaging approval. Cost tier: low.

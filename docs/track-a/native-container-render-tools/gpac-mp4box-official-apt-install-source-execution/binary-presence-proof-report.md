# Binary Presence Proof Report

Network-disabled container check result: `passed_after_quoting_retry`.

Checks proved:

- `/usr/share/keyrings/gpac-archive-keyring.gpg` exists.
- `/etc/apt/sources.list.d/gpac.sources` exists.
- `/etc/apt/preferences.d/gpac.pref` exists.
- `dpkg-query -W gpac` returned `gpac	26.02-rev0-g118e60a90-HEAD	arm64`.
- `command -v MP4Box` returned `/usr/bin/MP4Box`.
- `test -x "$(command -v MP4Box)"` passed.

The first container check attempt failed because of shell quoting around `dpkg-query -f`; the corrected `--network none` check passed. No `MP4Box -version`, MP4Box media command, GPAC command, file muxing, file probing, FFmpeg/FFprobe, render/export, or media processing ran.

`dpkg-query -L gpac` observed package-installed resource files inside the image. They were not generated by this repo, copied into this repo, staged, or committed.

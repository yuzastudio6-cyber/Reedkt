# AI Graphics Job Payload Dry-Run Gate Status Private Artifact

Decision: `worker_ai_graphics_metadata_job_payload_dry_run_gate_status_ready_with_warnings`

`privateArtifactDryRunGateStatusReady: true`

Every future payload remains required to use `<PRIVATE_ARTIFACT_MANIFEST_REF>` and `<CHECKSUM_REF>`. Public artifacts and signed URLs are not source truth for this lane.

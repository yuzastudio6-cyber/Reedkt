export interface MockProjectEditBriefScenario {
  id: string
  title: string
  expectedOk: true
  mockOnly: true
  summary: string
}

function scenario(
  id: string,
  title: string,
  summary: string,
): MockProjectEditBriefScenario {
  return {
    id,
    title,
    expectedOk: true,
    mockOnly: true,
    summary,
  }
}

export const MOCK_PROJECT_EDIT_BRIEF_SCENARIOS: MockProjectEditBriefScenario[] = [
  scenario('optional-brief-not-opened', 'Optional brief not opened fixture exists', 'Edit Brief can be absent while Edit Chat remains usable.'),
  scenario('active-empty-brief', 'Active brief with no markers exists', 'An opened Brief may have zero markers.'),
  scenario('broll-attached-marker', 'B-roll marker with attached mock clip exists', 'Metadata-only mock b-roll attachment is represented.'),
  scenario('broll-needs-asset', 'B-roll marker needing asset exists', 'Future upload/select requirement is modeled without upload.'),
  scenario('music-soundtrack-marker', 'Music/soundtrack marker exists', 'Soundtrack guidance is captured as planning metadata only.'),
  scenario('caption-text-marker', 'Caption/text marker exists', 'Caption guidance is captured without caption rendering.'),
  scenario('cut-remove-marker', 'Cut/remove marker exists', 'Range cut instruction is represented.'),
  scenario('do-not-use-marker', 'Do-not-use marker exists', 'Avoid guidance is represented as a marker priority.'),
  scenario('marker-chat-confirmation', 'Marker chat confirmation exists', 'Scoped marker confirmation is separate from main Edit Chat.'),
  scenario('marker-needs-clarification', 'Marker needing clarification exists', 'Clarification status blocks planner readiness.'),
  scenario('conflicting-music-no-music', 'Conflicting music/no-music markers exist', 'Conflict records require owner review.'),
  scenario('changed-after-plan', 'Changed-after-plan brief exists', 'Marker revision is represented after mock plan application.'),
  scenario('point-marker-timeline', 'Point marker maps to timeline point', 'Point markers keep only a start time.'),
  scenario('range-marker-timeline', 'Range marker maps to timeline range', 'Range markers keep start and end time.'),
  scenario('lane-broll', 'Marker lane maps broll correctly', 'B-roll markers map to the visual lane.'),
  scenario('lane-music', 'Marker lane maps music correctly', 'Music markers map to the audio lane.'),
  scenario('lane-caption', 'Marker lane maps caption correctly', 'Caption markers map to the text/graphics lane.'),
  scenario('icon-cut-remove', 'Marker icon maps cut/remove correctly', 'Cut/remove markers expose a compact icon label.'),
  scenario('color-conflict', 'Marker color maps conflict correctly', 'Conflict markers use the danger color token.'),
  scenario('status-draft', 'Marker status draft is supported', 'Draft marker state is represented.'),
  scenario('status-needs-clarification', 'Marker status needs_clarification is supported', 'Clarification marker state is represented.'),
  scenario('status-needs-asset', 'Marker status needs_asset is supported', 'Asset-needed marker state is represented.'),
  scenario('status-confirmed', 'Marker status confirmed is supported', 'Confirmed marker state is represented.'),
  scenario('status-ready-for-plan', 'Marker status ready_for_plan is supported', 'Planner-ready marker state is represented.'),
  scenario('status-conflict', 'Marker status conflict is supported', 'Conflict marker state is represented.'),
  scenario('status-applied-to-plan', 'Marker status applied_to_plan is supported', 'Applied marker state is represented.'),
  scenario('priority-must-follow', 'Marker priority must_follow is supported', 'Must-follow marker priority is represented.'),
  scenario('priority-should-follow', 'Marker priority should_follow is supported', 'Should-follow marker priority is represented.'),
  scenario('priority-optional', 'Marker priority optional is supported', 'Optional marker priority is represented.'),
  scenario('priority-avoid', 'Marker priority avoid is supported', 'Avoid marker priority is represented.'),
  scenario('ai-mode-off', 'AI mode off is supported', 'Marker AI can be disabled.'),
  scenario('ai-mode-confirm-only', 'AI mode confirm_only is supported', 'Confirm-only marker AI mode is represented.'),
  scenario('ai-mode-clarifying', 'AI mode ask_clarifying_questions is supported', 'Clarifying marker AI mode is represented.'),
  scenario('ai-mode-suggest-options', 'AI mode suggest_options is supported', 'Suggest-options marker AI mode is represented.'),
  scenario('message-user-note', 'Marker message user note exists', 'User marker note is stored in Marker Chat.'),
  scenario('message-assistant-confirmation', 'Marker message assistant confirmation exists', 'Assistant confirmation is scoped to marker.'),
  scenario('intent-add-broll', 'Structured intent add_broll exists', 'B-roll intent action is represented.'),
  scenario('intent-remove-or-cut', 'Structured intent remove_or_cut exists', 'Cut intent action is represented.'),
  scenario('intent-caption', 'Structured intent add_caption_or_text exists', 'Caption intent action is represented.'),
  scenario('intent-music', 'Structured intent add_music_or_soundtrack exists', 'Music intent action is represented.'),
  scenario('intent-avoid', 'Structured intent avoid_or_do_not_use exists', 'Avoid intent action is represented.'),
  scenario('audio-keep-original', 'Intent audio behavior keep original audio works', 'Audio behavior can preserve original audio.'),
  scenario('visual-insert-broll', 'Intent visual behavior insert broll works', 'Visual behavior can insert b-roll.'),
  scenario('caption-make-smaller', 'Intent caption behavior make smaller works', 'Caption behavior can request smaller text.'),
  scenario('attachment-broll-video', 'Attachment broll video exists', 'B-roll video attachment kind is represented.'),
  scenario('attachment-image', 'Attachment image exists', 'Image attachment kind is represented.'),
  scenario('attachment-music-track', 'Attachment music track exists', 'Music track attachment kind is represented.'),
  scenario('attachment-sfx', 'Attachment SFX exists', 'SFX attachment kind is supported by the type surface.'),
  scenario('attachment-reference-label', 'Attachment reference label exists', 'Reference labels can represent missing future assets.'),
  scenario('export-instagram-reel', 'Export setting Instagram Reel preset exists', 'Vertical delivery preset is represented.'),
  scenario('export-youtube-standard', 'Export setting YouTube standard preset exists', 'Wide delivery preset is represented.'),
  scenario('export-custom-future', 'Export setting custom future state exists', 'Custom export metadata is represented without rendering.'),
  scenario('drawer-marker-intent', 'Drawer model includes marker and intent', 'Drawer models join marker with structured intent.'),
  scenario('drawer-attachments', 'Drawer model includes attachments', 'Drawer models include metadata-only attachments.'),
  scenario('drawer-marker-chat', 'Drawer model includes marker chat messages', 'Drawer models include scoped Marker Chat messages.'),
  scenario('bundle-all-records', 'Bundle includes all records', 'Bundle records join brief, markers, messages, intents, conflicts, and export settings.'),
  scenario('brief-readable-summary', 'Brief readable summary works', 'Readable summary mapper emits compact text.'),
  scenario('intent-readable-summary', 'Intent readable summary works', 'Intent summary mapper emits compact text.'),
  scenario('export-summary', 'Export settings summary works', 'Export settings summary mapper emits compact text.'),
  scenario('debug-summary', 'Debug summary works', 'Debug summary reports counts and warnings.'),
  scenario('distinct-from-session', 'ProjectEditBrief remains distinct from ProjectEditSession', 'Brief is an optional layer inside the session, not the session itself.'),
  scenario('distinct-from-preference', 'ProjectEditBrief remains distinct from EditPreference', 'Brief is timeline instruction, not reusable style DNA.'),
  scenario('marker-chat-distinct', 'Marker Chat remains distinct from main Edit Chat', 'Marker Chat is scoped to one marker.'),
  scenario('fixtures-mock-only', 'Fixtures are mockOnly true', 'Every fixture record is mock-only.'),
  scenario('no-real-upload-marker', 'Fixtures have no real upload marker', 'Fixtures do not claim uploads or file-byte reads.'),
  scenario('no-provider-call-marker', 'Fixtures have no provider call marker', 'Fixtures do not claim provider/model calls.'),
  scenario('no-render-job-marker', 'Fixtures have no render job marker', 'Fixtures do not claim render jobs.'),
  scenario('no-credit-marker', 'Fixtures have no credit marker', 'Fixtures do not claim credit reservation or spend.'),
]
